# 个性化学习资源智能体系统 - 开发说明书

## 1. 项目概述

### 1.1 项目背景与意义

本项目针对高等教育阶段学生学习资源繁杂、难以个性化匹配、缺乏智能引导的核心痛点，利用大语言模型与多智能体协同技术，构建一个面向高校课程的个性化学习资源生成与辅导系统。系统以真实课程知识库（数据结构、高等数学等）为基础，通过自然语言对话构建学生画像，动态生成教学文档、思维导图、题库、代码示例、拓展阅读等多模态学习资源，并基于学生画像规划个性化学习路径，提供即时智能答疑。

### 1.2 系统目标

- 通过自然语言对话构建学生动态学习画像（不少于 6 个维度）
- 多智能体协同生成至少 5 种类型的个性化学习资源（教学文档、思维导图、题库、代码示例、拓展阅读）
- 基于学生画像生成个性化学习路径并推送资源
- 提供即时的智能辅导与答疑服务
- 对生成内容进行质量评估，形成持续优化闭环
- 提供美观、现代、交互友好的 Web 前端

### 1.3 技术栈

| 类别 | 技术/工具 | 说明 |
|------|----------|------|
| 开发语言 | Python 3.10+ | 推荐 3.12，标准库优先 |
| 前端框架 | Streamlit | 快速构建交互式 Web 应用 |
| AI 接口 | OpenAI 兼容 API | 支持科大讯飞星火、DeepSeek 等 |
| 数据存储 | SQLite 3 | 轻量级关系数据库，随 Python 发行 |
| 可视化渲染 | Graphviz | 思维导图 PNG 生成 |
| 配置管理 | python-dotenv（可选）+ 手动降级解析器 | 无第三方强依赖也可运行 |
| 样式 | 自定义 CSS | ChatGPT 风格深色侧栏 + 卡片式内容 |
| 多智能体框架 | 自研轻量调度器 | 不依赖 langchain，纯原生实现 |

---

## 2. 需求分析

### 2.1 功能需求分析

#### 2.1.1 对话式学习画像构建

**需求描述**：摒弃传统繁琐表单，学生仅需用自然语言描述自己的学习背景与目标，系统自动提取特征并结构化为不少于 6 个维度的学习画像。

**核心维度**（由 `ProfileAgent.run()` 指令驱动）：
- **专业**：学生所属专业领域（字符串）
- **知识水平**：基础薄弱 / 中等 / 基础扎实（字符串）
- **认知风格**：视觉型 / 听觉型 / 动手型 等（字符串）
- **薄弱环节**：易错点或薄弱知识点（字符串列表）
- **学习目标**：期望达成的学习目标（字符串列表）
- **学习建议**：针对性的学习建议（字符串列表）
- **可选字段**：兴趣方向、偏好资源类型、估计水平分数等

**技术要点**：
- 前端 `st.text_area` 接收中文自然语言输入 → `ProfileAgent` 调用 `BaseAIClient.generate_text()`
- AI 客户端严格要求 LLM 仅输出 JSON，禁止添加解释性文本
- `ProfileAgent` 端用 `json.loads()` 解析；失败时回退为 `{"raw": text}` 以便前端调试
- API 错误（密钥缺失、网络不通、鉴权失败）统一标记为 `{"_ai_error": True, ...}`
- 画像成功后由 `Database.add_student_profile()` 写入 `student_profiles` 表
- feat1–feat6 列存储数值化特征，`extra` 列以 JSON 字符串保存完整画像

#### 2.1.2 多智能体协同的资源生成

**需求描述**：系统采用多智能体架构，不同角色的子智能体协同生成至少 5 种类型的个性化资源。

**资源类型与负责子智能体**：

| 子智能体 | 资源类型 | 输出格式 | 核心方法 |
|---------|---------|---------|---------|
| `DocumentAgent` | 教学文档 | Markdown 文本 | `BaseAIClient.generate_text()` |
| `MindmapAgent` | 思维导图 | JSON 树结构 + PNG | `BaseAIClient.generate_mindmap()` → `ResourceGenerator.render_mindmap()` |
| `QuestionBankAgent` | 练习题库 | JSON 列表（q/a/explanation） | `BaseAIClient.generate_questions()` |
| `CodeAgent` | 代码示例 | 带注释的代码文本 | `BaseAIClient.generate_code()` |
| `ReadingMaterialAgent` | 拓展阅读 | JSON 列表（title/type/summary/difficulty/order/link） | `BaseAIClient.generate_reading_material()` |

**知识库增强机制**：
- `resource_subagents._kb_context(topic)` 在生成前调用 `KnowledgeBase.search_knowledge_point()`，将课程知识库中与主题相关的章节摘要、知识点内容拼接到 prompt 中
- 这一步极大增强了生成内容与具体课程的贴合度，减少 "幻觉"
- 对 AI 返回的 JSON 提供宽容解析：支持前后带解释文本、单引号风格、`nodes`/`children` 字段差异等多种非标准格式

**持久化与下载**：
- 思维导图由 `ResourceGenerator`（基于 Graphviz 的 `Digraph`）将 JSON 树渲染为 PNG，并保存在 `data/generated/` 目录
- 前端对每种资源均提供 `st.download_button`，可下载 JSON、文本或 PNG

#### 2.1.3 个性化学习路径规划和资源推送

**需求描述**：基于学生画像与当前学习主题，生成阶段式、有序列的学习路径。

**技术要点**：
- `PathAgent.run(profile_or_topic)` 调用 `BaseAIClient.generate_text()`，并在 prompt 中结合画像信息
- 生成结果为 Markdown 文本，前端以卡片形式展示
- 成功后由 `Database.add_learning_step(user_id, step, progress, details)` 逐条写入 `learning_paths` 表
- 前端在生成路径前校验 `st.session_state.profile` 是否存在，缺失时以橙色提示引导用户先生成画像

#### 2.1.4 智能辅导

**需求描述**：学生可针对任意学习问题发起问答，系统提供即时、多轮、Markdown 格式的答疑。

**技术要点**：
- `TutoringAgent.run(question)` 直接调用 `generate_text()`
- 前端用 `st.chat_message("user")` / `st.chat_message("assistant")` 展示对话气泡
- 对话历史存储在 `st.session_state.chat_history`，支持页面切换后保留上下文
- 支持代码块、列表、引用等 Markdown 渲染

#### 2.1.5 学习效果评估（加分项）

**需求描述**：对生成的教学文档进行质量评估，为后续优化提供反馈依据。

**技术要点**：
- `EvaluationAgent.run(document_text)` 以结构化提示要求 LLM 从准确性、完整性、可读性、难度匹配度等维度打分并给出建议
- 评估结果写入 `Database.add_resource()` 的 metadata 字段，便于后续统计分析

### 2.2 非功能性需求分析

#### 2.2.1 用户界面与交互

- **界面风格**：ChatGPT 风格深色侧栏 + 浅色主内容区；各功能页面有渐变色标题栏
- **卡片式展示**：所有资源、画像、路径、章节等均以圆角卡片呈现，带阴影与 hover 位移
- **响应式布局**：适配桌面端主流分辨率（1366×768、1920×1080）
- **流式交互**：AI 调用期间显示 `st.spinner`，防止白屏卡顿感
- **错误反馈**：API/配置错误以红色横幅（`st.error`）明确提示并给出修复指引

#### 2.2.2 内容安全与防幻觉

- **知识库锚定**：`_kb_context()` 将课程知识库内容注入到生成 prompt，从源头降低事实错误
- **结构化输出**：严格要求 JSON 输出；非 JSON 回退为原始文本，不伪造结构
- **显式错误标记**：`[AI错误]:<错误类型>` 前缀，前端统一识别
- **敏感词过滤**：AI 服务商端提供基础内容安全，系统层不在本地维护敏感词表以避免反向依赖

#### 2.2.3 性能与响应

- **启动时间**：Streamlit 冷启动 < 10 秒；知识库首次加载 < 2 秒
- **AI 调用**：单次画像生成 < 15 秒；单种资源生成 < 20 秒；辅导问答 < 10 秒（实际取决于后端模型）
- **资源渲染**：思维导图 Graphviz PNG 渲染 < 1 秒
- **数据库操作**：SQLite `timeout=30`，`INSERT OR REPLACE` 幂等写入

#### 2.2.4 可扩展性与可维护性

- **AI 接口抽象**：`BaseAIClient` 抽象基类声明 5 个核心方法；新增模型只需实现一个子类即可接入，无需改动 agent 与前端
- **多课程架构**：`data/course_kb/<course_id>/` 目录化管理，新增课程只需新建子目录 + `course_manifest.json` + 章节 JSON
- **缓存机制**：`_course_cache` 字典缓存已加载课程知识库实例，避免重复磁盘 IO
- **无外部强依赖**：`python-dotenv` 可选，缺失时 `ai_client._load_env()` 降级为手动解析

---

## 3. 系统架构设计

### 3.1 总体架构图

```
                    ┌──────────────────────────────┐
                    │        Streamlit 前端         │
                    │  (src/frontend/app.py)       │
                    │  ┌────────┬────────┬──────┐ │
                    │  │画像构建│资源生成│路径...│ │
                    │  └────────┴────────┴──────┘ │
                    └──────────────┬───────────────┘
                                   │
                                   ▼
                    ┌──────────────────────────────┐
                    │   MultiAgentScheduler         │
                    │   (src/agents/...)           │
                    │  ┌──────────────────────┐    │
                    │  │ ProfileAgent         │    │
                    │  │ ResourceAgent(→5子) │    │
                    │  │ PathAgent            │    │
                    │  │ TutoringAgent        │    │
                    │  │ EvaluationAgent      │    │
                    │  └──────────────────────┘    │
                    └──────────────┬───────────────┘
         ┌─────────────────────────┼───────────────────────────┐
         ▼                         ▼                           ▼
┌──────────────────┐   ┌──────────────────────┐   ┌──────────────────┐
│  BaseAIClient    │   │    KnowledgeBase     │   │    Database      │
│  (OpenAI兼容)    │   │  (课程JSON知识库)     │   │   (SQLite)       │
│  mock / openai   │   │  data/course_kb/*/   │   │  student_profile │
└────────┬─────────┘   └──────────────────────┘   │  learning_path   │
         │                                         │  resources       │
         ▼                                         └──────────────────┘
┌────────────────────────────────────────────┐
│          大模型服务（讯飞星火 / 任何         │
│          OpenAI 兼容端点）                  │
└────────────────────────────────────────────┘
```

### 3.2 模块职责说明

#### 3.2.1 `MultiAgentScheduler` — 多智能体调度器

**文件位置**：`src/agents/multi_agent_scheduler.py`

**核心职责**：
- 持有对各智能体的引用，对外暴露统一入口
- `run_full_flow(prompt, topic)`：一键跑完画像→资源→路径→辅导→评估的全流程
- `execute_task(task_name, payload)`：按任务名（`profile`/`resource`/`path`/`tutoring`）分发，前端页面仅通过此方法调用智能体
- 异常捕获并返回 `{"error": "..."}`，避免前端因未捕获异常崩溃

#### 3.2.2 `ProfileAgent` — 画像智能体

**文件位置**：`src/agents/profile_agent.py`

**核心职责**：
- 构造严格指令要求 LLM 只返回中文 JSON
- 检测 `[AI错误]` 前缀，统一转为 `{"_ai_error": True, ...}`
- 对合法 JSON 做 `json.loads`；失败时保留原始文本便于调试

#### 3.2.3 `ResourceAgent` — 资源生成总控

**文件位置**：`src/agents/resource_agent.py` + `src/agents/resource_subagents.py`

**核心职责**：
- `ResourceAgent.run(topic)` 并行（同步）调用 5 个子智能体并汇总返回字典
- **`DocumentAgent`**：结合课程知识库生成 Markdown 教学文档
- **`MindmapAgent`**：生成 `{"title", "children": [...]}` 树形 JSON；对一级节点一次性补全 `note` 和 2 个三级子节点
- **`QuestionBankAgent`**：生成 `[{q, a, explanation}, ...]` 结构化题目
- **`CodeAgent`**：生成 Python 可运行代码示例，含注释
- **`ReadingMaterialAgent`**：生成拓展阅读列表，含难度、摘要、推荐理由等

#### 3.2.4 `PathAgent` — 学习路径智能体

**文件位置**：`src/agents/path_agent.py`

**核心职责**：
- 基于学生画像 + 当前课程/主题生成阶段性学习计划 Markdown
- 结果可写入 `learning_paths` 表持久化

#### 3.2.5 `TutoringAgent` — 辅导答疑智能体

**文件位置**：`src/agents/tutoring_agent.py`

**核心职责**：
- 接收学生问题，调用 `generate_text()` 返回 Markdown 解答
- 前端维护多轮对话，后续可扩展将历史上下文一并传入

#### 3.2.6 `EvaluationAgent` — 质量评估智能体

**文件位置**：`src/agents/evaluation_agent.py`

**核心职责**：
- 对教学文档做结构化评分与改进建议输出
- 作为系统优化闭环的数据源

### 3.3 AI 客户端层

#### 3.3.1 `BaseAIClient` — 接口抽象

**文件位置**：`src/api/base.py`

声明以下抽象方法，子类必须实现：
- `generate_text(prompt, system_msg=None) -> str`
- `generate_mindmap(topic) -> Dict`
- `generate_code(topic, language="python") -> str`
- `generate_questions(topic, num=5) -> List[Dict]`
- `generate_reading_material(topic, num=5) -> List[Dict]`

#### 3.3.2 `OpenAIClient` — OpenAI 兼容实现

**文件位置**：`src/api/openai_client.py`

**关键特性**：
- 从环境变量 / `config/.env` 读取 `OPENAI_API_KEY`、`OPENAI_API_URL`、`OPENAI_MODEL`
- 默认 URL：`https://api.openai.com/v1/chat/completions`，支持替换为讯飞、DeepSeek、通义千问等兼容端点
- 通过 `requests.post` 调用，超时 120 秒
- 统一错误标记：文本型错误以 `[AI错误]:<错误类型>` 开头；结构化错误以 `{"_ai_error": True, "error_kind": ..., "detail": ...}` 形式返回
- JSON 宽容解析：`_extract_json_like()` + 单引号修正，兼容模型偶尔输出的非纯 JSON 格式
- 支持 `OPENAI_SYSTEM_MESSAGE` 自定义系统提示

**错误类型常量**（src/api/openai_client.py）：
- `ERR_MISSING_CONFIG` — 缺少 API 密钥或 URL
- `ERR_CONNECTION` — 网络连接失败 / 超时
- `ERR_AUTH` — HTTP 401/403 鉴权失败
- `ERR_HTTP` — 其他 HTTP 状态码错误
- `ERR_PARSE` — JSON 解析失败
- `ERR_EMPTY` — 内容为空
- `ERR_UNKNOWN` — 兜底错误

#### 3.3.3 `MockClient` — 本地离线 mock

**文件位置**：`src/api/mock_client.py`

不依赖任何网络，使用正则规则从用户输入中提取信息，生成合理的 JSON 画像与示例资源。主要用于：
- 无密钥环境下的系统冒烟测试
- 前端页面联调
- CI/CD 流水线自动化测试

#### 3.3.4 `ai_client.get_ai_client()` — 工厂函数

**文件位置**：`src/api/ai_client.py`

- 读取 `config/.env`（支持 python-dotenv 与手动解析两种方式）
- 注入到 `os.environ`
- 构造 `OpenAIClient` 实例并返回
- 日志打印配置项（API 密钥脱敏显示中间部分）

### 3.4 核心数据模块

#### 3.4.1 `KnowledgeBase` — 课程知识库

**文件位置**：`src/core/knowledge_base.py`

**目录结构**：
```
data/course_kb/
├── advanced_mathematics/          # 高等数学（8 章）
│   ├── course_manifest.json       # 课程清单
│   ├── ch01_function_limit.json   # 函数与极限
│   ├── ch02_derivative_differential.json
│   ├── ch03_mvt_applications.json
│   ├── ch04_indefinite_integral.json
│   ├── ch05_definite_integral.json
│   ├── ch06_differential_equation.json
│   ├── ch07_multivariable_calculus.json
│   └── ch08_multivariable_integration.json
└── data_structures/               # 数据结构（9 章 + 1 补充）
    ├── course_manifest.json
    ├── ch01_introduction.json
    ├── ch02_linear_list.json
    ├── ch03_stack_queue.json
    ├── ch04_string_array.json
    ├── ch05_tree.json
    ├── ch06_graph.json
    ├── ch07_search.json
    ├── ch08_sort.json
    ├── ch09_file.json
    └── ch_supplemental.json
```

**`course_manifest.json` 字段**：
- `course_name`：中文课程名
- `course_name_en`：英文课程名
- `target_major`：目标专业
- `semester`：学期
- `total_hours`：总学时
- `chapters[]`：`{id, title, hours, file, knowledge_points[]}`

**章节 JSON 字段**：
- `summary`：章节摘要
- `key_points[]`：`{name, content, difficulty}`
- `code_examples[]`：`{title, code, explanation}`
- `questions[]`：`{q, a, explanation}`
- `reading_materials[]`：`{title, type, summary, difficulty, order, link}`

**关键方法**：
- `list_courses()`：扫描 `data/course_kb/` 目录并返回课程列表
- `get_knowledge_base(course_id)`：获取指定课程实例，带 `_course_cache` 缓存
- `list_chapters()`：章节元数据列表
- `get_chapter(id_or_file)`：章节完整内容
- `search_knowledge_point(keyword)`：关键词匹配章节摘要 + 知识点
- `get_questions(chapter)`：查询练习题
- `get_code_examples(chapter)`：查询代码示例
- `get_reading_materials(chapter)`：查询拓展阅读
- `build_prompt_context(topic, max_chars=2000)`：为 AI 构建参考上下文文本

#### 3.4.2 `Database` — SQLite 持久化

**文件位置**：`src/core/database.py`

**数据表设计**：

**表 `student_profiles`**：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 |
| user_id | TEXT | UNIQUE NOT NULL | 用户标识 |
| feat1–feat6 | REAL |  | 6 个数值化特征 |
| extra | TEXT |  | JSON 格式完整画像 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**表 `learning_paths`**：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 |
| user_id | TEXT | NOT NULL | 用户标识 |
| step | INTEGER | NOT NULL | 学习步骤序号 |
| progress | REAL | DEFAULT 0.0 | 进度百分比 |
| details | TEXT |  | 路径详细内容 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |
| — | — | UNIQUE(user_id, step) | 同用户同步骤唯一 |

**表 `resources`**：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 |
| user_id | TEXT |  | 用户标识 |
| resource_id | TEXT | UNIQUE | 资源标识 |
| resource_type | TEXT |  | mindmap/document/code/... |
| content | TEXT |  | 资源内容（文本或 JSON） |
| metadata | TEXT |  | JSON 元数据 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**关键方法**（完整 CRUD）：
- `create_tables()`：三张表一次性创建
- `add_student_profile(user_id, features, extra)` / `get_student_profile` / `update_student_profile` / `delete_student_profile` / `list_student_profiles`
- `add_learning_step(user_id, step, progress, details)` / `get_learning_path` / `update_learning_step` / `delete_learning_step`
- `add_resource(user_id, resource_id, resource_type, content, metadata)` / `get_resource` / `list_resources` / `update_resource` / `delete_resource`

**实现要点**：
- `sqlite3.connect(self.db_path, timeout=30)` 支持长时间锁定下的并发访问
- `INSERT OR REPLACE` 实现 upsert 语义，幂等写入
- `row_factory = sqlite3.Row` 便于字典式访问
- `_row_to_dict()` 自动将 `extra` 与 `metadata` 列反序列化为 Python dict

#### 3.4.3 `ResourceGenerator` — 资源渲染器

**文件位置**：`src/core/resource_generator.py`

**思维导图渲染流程**：
1. 接收 `{"title": "...", "children": [...]}` JSON 树
2. 递归遍历并向 `graphviz.Digraph` 添加节点与边（`rankdir=LR` 横向布局）
3. 对超长 `title` 做合理换行，防止节点过宽
4. 渲染为 PNG 文件（路径：`data/generated/<slug>.png`），同时保存 `.dot` 源文件
5. 返回文件路径供前端 `st.image()` 预览和 `st.download_button()` 下载

**容错**：
- Graphviz 未安装时捕获 `ExecutableNotFound` 异常并回退展示原始 JSON，不崩溃
- JSON 结构非法时降级为单层节点渲染

---

## 4. 前端界面设计（Streamlit）

### 4.1 技术选型与布局

**选择 Streamlit 的原因**：
- Python 生态原生支持，代码量少、开发速度快
- 内置组件（text_input、button、image、download_button、markdown、chat_message）覆盖本项目需求
- `session_state` 机制原生支持会话级状态管理
- 热重载：代码修改后自动刷新页面，便于快速迭代

**页面结构**：
```
┌────────────────────────────────────────────────┐
│  Streamlit Sidebar（深色）                      │
│  ┌────────────────────────────────┐            │
│  │  📚 个性化学习智能体  (标题)    │            │
│  ├────────────────────────────────┤            │
│  │  👤 画像构建                    │ ← 默认页   │
│  │  📦 多模态资源生成              │            │
│  │  🗺️ 个性化学习路径              │            │
│  │  💬 智能辅导                    │            │
│  │  📚 课程知识库                  │            │
│  │  ⚙️ 系统设置（API配置）          │            │
│  └────────────────────────────────┘            │
└────────────────────────────────────────────────┘
                         │
                         ▼
               ┌──────────────────┐
               │  主内容区（白）   │
               │  · 渐变色标题卡片 │
               │  · 内容卡片列表   │
               │  · 交互控件       │
               └──────────────────┘
```

### 4.2 各页面交互流程

#### 4.2.1 画像构建页面
1. `st.text_area` 接收学生描述
2. 点击 **"生成画像"** → `st.spinner` 显示加载状态
3. `scheduler.execute_task("profile", text)` → `ProfileAgent` → AI 调用
4. 成功：以卡片形式 `st.write` 展示 6+ 维画像信息
5. 调用 `db.add_student_profile(user_hash, features, full_profile)` 持久化
6. 将画像字典存入 `st.session_state.profile`，供路径页面使用
7. 失败：`st.error` 红色横幅显示具体错误类型（`ERR_MISSING_CONFIG` / `ERR_CONNECTION` 等）

#### 4.2.2 多模态资源生成页面
1. 顶部显示当前选中课程（读取自 `st.session_state.current_course_id`）
2. 文本框输入知识点关键词，下拉选择资源类型（`document` / `mindmap` / `question_bank` / `code` / `reading_material`）
3. 点击 **"🚀 生成资源"** → 调用 `scheduler.execute_task("resource", {"kp": topic})`
4. **思维导图**：
   - 解析 JSON → `rg.render_mindmap()` → PNG
   - `st.image()` 预览 + `st.download_button()` 下载
5. **题库**：
   - 遍历题目列表，`st.expander` 折叠答案
   - 展开时显示 `a` 字段（答案）与 `explanation`（详解）
6. **拓展阅读**：
   - 卡片式展示（标题、类型、难度、推荐理由）
   - 按 `order` 升序排列
7. **教学文档 / 代码**：`st.markdown` 渲染

#### 4.2.3 个性化学习路径页面
1. 检查 `st.session_state.profile`
2. 不存在 → `st.warning` 提示"请先生成画像"
3. 存在 → `scheduler.execute_task("path", profile)` 调用 PathAgent
4. 生成 Markdown 计划卡片展示
5. 按步骤写入 `learning_paths` 表

#### 4.2.4 智能辅导页面
1. 类 ChatGPT 对话界面：
   - 用户气泡右侧（紫色）
   - AI 气泡左侧（灰色）
2. `st.chat_input()` 按回车发送
3. 对话历史保存在 `st.session_state.chat_history`
4. AI 回复通过 `TutoringAgent.run(question)` 获取，Markdown 渲染
5. 支持代码块、列表、公式、引用等格式

#### 4.2.5 课程知识库页面
1. 顶部 `st.selectbox` 显示 `list_courses()` 结果供切换
2. 切换后写回 `st.session_state.current_course_id`，影响资源生成页的课程上下文
3. 课程信息卡片：课程名 / 英文名 / 目标专业 / 总学时
4. 统计卡片：章节数 / 知识点数 / 代码示例数 / 练习题数
5. `st.text_input("🔍 搜索关键词")` 触发 `KnowledgeBase.search_knowledge_point()`
6. 章节列表：每章用 `st.expander` 折叠，展开后显示摘要 + 知识点 + 代码 + 题目 + 阅读材料

#### 4.2.6 系统设置页面
- API Key / Model / URL 三项文本框
- 保存后写入 `config/.env`
- 下次重启应用或点击"重新加载客户端"按钮后生效

### 4.3 样式设计规范

**渐变色方案**（页面顶部 Banner）：

| 页面 | 起始色 | 结束色 |
|------|--------|--------|
| 画像构建 | `#667eea` | `#764ba2` |
| 资源生成 | `#f093fb` | `#f5576c` |
| 学习路径 | `#4facfe` | `#00f2fe` |
| 智能辅导 | `#43e97b` | `#38f9d7` |
| 课程知识库 | `#667eea` | `#764ba2` |
| 系统设置 | `#fcb045` | `#fd1d1d` |

**通用样式类**：
- `.card`：白色卡片（`border-radius: 12px`, `box-shadow: 0 2px 8px rgba(0,0,0,.06)`）
- `.tag-easy` / `.tag-normal` / `.tag-hard`：难度标签（绿/橙/红）
- `.btn-primary`：紫色渐变按钮（hover 位移 2px）
- `.kp-box` / `.code-box` / `.q-box`：知识点 / 代码 / 题目容器

### 4.4 会话状态管理

`st.session_state` 维护以下全局状态：
- `profile`：当前已生成的画像 dict
- `chat_history`：辅导对话历史列表 `[{"role": "user"|"assistant", "content": ...}]`
- `current_course_id`：当前选中课程（默认第一门）
- `ai`：`OpenAIClient` 单例
- `scheduler`：`MultiAgentScheduler` 单例
- `db`：`Database` 单例
- `rg`：`ResourceGenerator` 单例

**初始化入口**：应用启动时 `src/frontend/app.py` 顶部代码块 `if "ai" not in st.session_state: ...` 完成对象构造并缓存。

---

## 5. 创新实践与用户体验优化策略

### 5.1 多课程目录化知识库架构

**创新点**：
- `data/course_kb/<course_id>/` 目录化组织，新增课程仅需新增子目录
- `course_manifest.json` 统一描述元数据，支持中文/英文课程名、目标专业、学时
- `list_courses()` 动态扫描，前端无需硬编码课程列表
- `_course_cache` 字典缓存已加载实例，避免重复磁盘 IO

**实现要点**：
- 顶层函数 `list_courses()` 与类 `KnowledgeBase` 分离，前者负责扫描，后者负责单课程内容装载
- `get_knowledge_base(course_id)` 工厂函数完成缓存命中与实例化

### 5.2 轻量多智能体任务调度框架

**创新点**：
- 不依赖 langchain/anyio 等重型框架，纯 Python 原生实现
- `MultiAgentScheduler.execute_task(task_name, payload)` 为前端提供统一 RPC 风格入口
- 前端对各智能体无感知，新增任务类型只需在 scheduler 加一个分支即可

**实现要点**：
- 每个智能体类只暴露一个 `run(...)` 方法，接口风格统一
- scheduler 捕获异常并以 `{"error": "..."}` 回传，前端只需判断 key 是否存在

### 5.3 结构化输出与多步骤可视化渲染

**创新点**：
- 对 AI 返回做 "严格 JSON 指令 + 宽容解析" 双层策略：prompt 要求纯 JSON，解析时允许前后带解释文本
- 思维导图实现了 "LLM 生成骨架 → 一次性补全节点 note 与子节点 → Graphviz 渲染 PNG" 的三阶段流水线
- 每种资源类型在前端有独立渲染器（非一刀切文本），并提供下载能力

### 5.4 错误处理与用户提示闭环

**创新点**：
- API 层错误统一标记 `[AI错误]:<错误类型>` / `{"_ai_error": True}`
- 前端识别后以 `st.error` 红色横幅 + 可折叠原始详情展示
- 配置缺失场景给出明确修复指引（"请在右侧系统设置填入 API Key"）
- AI 层从不伪造假内容回传，确保用户能区分"生成成功"与"调用失败"

### 5.5 AI 接口抽象与可插拔模型接入

**创新点**：
- `BaseAIClient` 抽象基类声明固定方法签名
- `OpenAIClient` 兼容科大讯飞星火、DeepSeek、通义千问等所有 OpenAI 风格 API
- `MockClient` 本地离线 mock，便于开发测试与 CI
- 切换模型只需要修改 `config/.env`，无需重启代码

### 5.6 配置加载无第三方强依赖

**创新点**：
- 优先使用 `python-dotenv`；若未安装，降级为手动逐行解析
- 解析后同步写入 `os.environ`，保证下游 `os.getenv("OPENAI_API_KEY")` 也能读到值
- 密钥日志输出时脱敏：保留前 4 + 后 4 字符，中间用 `****` 替代

---

## 6. 数据流向与业务流程

### 6.1 学生画像构建流程

```
学生输入自然语言（中文）
      │
      ▼
st.text_area + 按钮 → st.session_state
      │
      ▼
ProfileAgent.run(prompt)
  ├─ 构造严格指令（要求纯JSON、中文、6+维度）
  └─ 调用 BaseAIClient.generate_text()
      ├─ OpenAIClient._build_payload()
      ├─ OpenAIClient._post()  →  大模型服务
      └─ _format_response_text()  →  str
      │
      ▼
json.loads() 解析 → dict
  ├─ 成功 → 结构化画像
  └─ 失败 → {"raw": text} 或 {"_ai_error": True}
      │
      ▼
前端 st.markdown 卡片展示
      │
      ▼
Database.add_student_profile(user_id, features, extra)
  ├─ user_id = hash(输入文本)
  ├─ feat1-feat6 = 数值化特征
  └─ extra = json.dumps(完整画像)
```

### 6.2 资源生成流程（以思维导图为例）

```
学生输入知识点 + 选择课程
      │
      ▼
MindmapAgent.run(topic)
  ├─ _kb_context(topic) → 注入课程知识库相关内容
  ├─ BaseAIClient.generate_mindmap(topic + context)
  ├─ JSON 宽容解析（children/nodes/单引号/带解释文本）
  └─ 对一级节点一次性补全 note + 2 个子节点（调用 generate_text）
      │
      ▼
ResourceGenerator.render_mindmap(structure, output_name)
  ├─ graphviz.Digraph(rankdir=LR)
  ├─ 递归写入节点/边
  └─ render → data/generated/<topic>.png
      │
      ▼
前端 st.image() 预览 + st.download_button() 下载
      │
      ▼
Database.add_resource(user_id, resource_id, "mindmap", png_path, metadata)
```

### 6.3 学习路径生成流程

```
检查 st.session_state.profile
      │
      ├─ 不存在 → st.warning 提示先生成画像
      │
      ▼
PathAgent.run(profile)
  └─ 以画像信息为上下文调用 generate_text()
      │
      ▼
Markdown 阶段式学习计划（基础 → 核心 → 综合提升）
      │
      ▼
Database.add_learning_step(user_id, step, 0.0, details)
```

### 6.4 智能辅导对话流程

```
学生输入问题（st.chat_input）
      │
      ▼
追加到 st.session_state.chat_history
      │
      ▼
TutoringAgent.run(question)
  └─ generate_text(question, system_msg="你是一个专业的学习助教...")
      │
      ▼
Markdown 解析 → HTML / st.chat_message("assistant").markdown()
      │
      ▼
追加到 chat_history （持久化在 session，不写入 DB，可扩展）
```

---

## 7. 部署与运行说明

### 7.1 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Ubuntu 20.04+ / Windows 10+ / macOS 12+ |
| Python 版本 | 3.10 或更高（推荐 3.12） |
| 系统依赖 | graphviz（思维导图渲染） |
| 浏览器 | Chrome 120+ / Firefox 120+ / Edge 120+ / Safari 17+ |
| 网络 | 可访问配置的 AI API 端点 |

### 7.2 安装步骤

**步骤 1：克隆或解压项目**
```bash
cd /path/to/workspace
# 已解压则直接进入 A3-main
cd A3-main
```

**步骤 2（可选）：创建虚拟环境**
```bash
python -m venv .venv
source .venv/bin/activate     # Linux / macOS
# Windows: .venv\Scripts\activate
```

**步骤 3：安装 Python 依赖**
```bash
pip install streamlit requests graphviz
# 可选，加速 env 解析：
pip install python-dotenv
```

**步骤 4：安装系统级 Graphviz**
```bash
# Ubuntu / Debian
sudo apt-get install graphviz

# CentOS / RHEL
sudo yum install graphviz

# macOS（Homebrew）
brew install graphviz

# Windows：从 https://graphviz.org/download/ 下载安装包
```

**步骤 5：配置 API 密钥**

方式 A：启动后访问 **⚙️ 系统设置** 页面可视化填写  
方式 B：直接编辑 `config/.env`：
```
OPENAI_API_KEY=sk-your-actual-key
OPENAI_MODEL=generalv3.5     # 讯飞星火示例
OPENAI_API_URL=https://spark-api-open.xf-yunai.cn/v1/chat/completions
# 自定义系统提示（可选）
OPENAI_SYSTEM_MESSAGE=你是一个专业的中文学习助教...
```

### 7.3 启动应用

```bash
cd /home/hjj/桌面/t/A3-main
streamlit run src/frontend/app.py
```

启动后终端显示：
```
  You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://<your-ip>:8501
```

浏览器打开 `http://localhost:8501` 即可访问。

### 7.4 验证知识库

**命令行快速自检**：
```bash
python -m src.core.knowledge_base
```
预期输出包含两门课程与章节、知识点搜索命中信息。

**页面验证**：
1. 访问 **📚 课程知识库** 页面
2. 顶部下拉切换 "数据结构" / "高等数学"
3. 展开任意章节，确认摘要、知识点、代码、题目、阅读材料均正常显示

### 7.5 目录结构总览

```
A3-main/
├── src/
│   ├── agents/                           # 多智能体模块
│   │   ├── __init__.py
│   │   ├── multi_agent_scheduler.py      # 任务调度器（核心）
│   │   ├── profile_agent.py              # 画像智能体
│   │   ├── resource_agent.py             # 资源生成总控
│   │   ├── resource_subagents.py         # 5 个子智能体（文档/导图/题库/代码/阅读）
│   │   ├── path_agent.py                 # 学习路径智能体
│   │   ├── tutoring_agent.py             # 辅导答疑智能体
│   │   └── evaluation_agent.py           # 质量评估智能体
│   ├── api/                              # AI 客户端
│   │   ├── __init__.py
│   │   ├── ai_client.py                  # 配置加载与客户端工厂
│   │   ├── base.py                       # 客户端抽象基类（接口规范）
│   │   ├── openai_client.py              # OpenAI 兼容实现（生产用）
│   │   └── mock_client.py                # 本地离线 mock（测试用）
│   ├── core/                             # 核心数据模块
│   │   ├── __init__.py
│   │   ├── database.py                   # SQLite 封装（3 张表 CRUD）
│   │   ├── knowledge_base.py             # 多课程 JSON 知识库管理
│   │   └── resource_generator.py         # 思维导图 PNG 渲染
│   └── frontend/
│       └── app.py                        # Streamlit 主应用（6 个页面）
├── data/
│   ├── course_kb/                        # 课程知识库数据
│   │   ├── data_structures/              # 数据结构课程（9 章 + 1 补充）
│   │   └── advanced_mathematics/         # 高等数学课程（8 章）
│   ├── generated/                        # 生成文件存储目录（思维导图 PNG）
│   └── user_profiles/                    # SQLite profiles.db 存储目录
├── config/
│   └── .env                              # API 配置（需用户自行创建或在页面填写）
├── 系统开发说明书.md                      # 本文档
├── 测试说明书.md                          # 测试文档
└── README.md
```

---

## 8. 关键技术实现细节

### 8.1 JSON 安全解析与宽容降级

**问题**：LLM 有时会在 JSON 前后添加解释性文本、代码块标记，或将 `children` 写成 `nodes`，甚至使用单引号。

**解决方案**：
- `OpenAIClient._extract_json_like(text)`：用正则 `r"(\{.*\}|\[.*\])"` 贪婪提取最外层 JSON 对象/数组
- `MindmapAgent`：同时支持 `children` 和 `nodes` 两种字段，自动归一化
- `ProfileAgent`：`json.loads` 失败时回退为 `{"raw": text}`，不崩溃
- `generate_questions` / `generate_reading_material`：成功时解析为 list，失败时返回结构化错误 dict，前端能分辨

### 8.2 AI 客户端错误标记协议

**问题**：前端需要区分 "AI 生成成功" 与 "API 调用失败"，同时保留失败原因以便调试。

**解决方案**：
- 文本型返回：`[AI错误]:<错误类型> <详情文本>`
- 结构化返回：`{"_ai_error": True, "error_kind": "...", "detail": "...", "topic": "..."}`
- 前端统一：检测 `[AI错误]` 前缀或 `_ai_error` key，红色横幅展示详情

### 8.3 Streamlit 会话保持与单例对象

**问题**：Streamlit 每次交互都会重跑整个 Python 文件（顶部到 main），直接构造对象会重复创建。

**解决方案**：
- 在 `src/frontend/app.py` 顶部写一段初始化代码：
```python
if "ai" not in st.session_state:
    st.session_state.ai = get_ai_client()
    st.session_state.scheduler = MultiAgentScheduler(st.session_state.ai)
    st.session_state.db = Database()
    st.session_state.rg = ResourceGenerator(...)
```
- 后续所有页面通过 `st.session_state.ai` / `.scheduler` / `.db` / `.rg` 引用，避免重复创建

### 8.4 SQLite 并发与幂等写入

**问题**：多用户或多会话同时写入 SQLite 可能触发锁定或主键冲突。

**解决方案**：
- `connect(..., timeout=30)` 提供充足等待时间
- 使用 `INSERT OR REPLACE` 而非 `INSERT`，对 `(user_id, step)`、`resource_id` 等唯一键天然支持 upsert
- 读操作并发安全（SQLite 多版本并发控制），对本项目规模完全够用

### 8.5 Graphviz 中文渲染

**问题**：默认 Graphviz 渲染中文可能出现乱码或方框。

**解决方案**：
- 确保系统安装中文字体（Noto CJK / WenQuanYi / 系统默认中文字体）
- Python 源文件使用 `# -*- coding: utf-8 -*-` 或 `from __future__ import annotations`
- 节点文本先做换行处理（每 15–20 字插入 `\n`）避免节点过宽

---

## 9. 扩展与优化方向

| 方向 | 说明 | 优先级 |
|------|------|--------|
| **异步流式生成** | 当前为同步阻塞调用；可改造为 `asyncio` + `st.write_stream` 实现打字机效果 | 高 |
| **更多资源类型** | PPT 大纲生成、短视频脚本、语音讲解 TTS、公式图像（LaTeX → SVG） | 中 |
| **学习进度追踪** | 记录用户点击、停留、练习完成度，写入数据库并提供可视化 Dashboard | 高 |
| **画像动态更新** | 随用户学习行为与答题表现自动重算画像特征，实现因材施教闭环 | 高 |
| **多用户系统** | 引入登录鉴权（Auth0/自建），数据按 user_id 严格隔离 | 中 |
| **缓存机制** | 对相同 prompt 的 LLM 调用结果做 Redis 缓存，减少重复请求 | 中 |
| **Prompt 管理** | 将各 agent 的 prompt 模板抽离为 `prompts/*.txt`，便于运营调优 | 低 |
| **单元测试覆盖** | 为所有 agent / core 模块编写 pytest 单元测试 | 中 |
| **容器化部署** | Dockerfile + docker-compose 一键部署 | 低 |
| **移动端适配** | 当前以桌面端为主；可针对窄屏优化侧栏折叠、布局密度 | 低 |

---

*文档版本：v2.0*  
*最后更新：2026 年 6 月*