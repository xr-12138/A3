# 个性化学习资源智能体系统 - 开发说明书

## 1. 项目概述

### 1.1 项目背景与意义

在数字化与智能化深度融合的时代，高等教育的个性化变革成为核心发展方向。传统教学模式难以满足不同学生在知识基础、学习能力、兴趣方向上的显著差异。本项目借助大模型技术体系，融合前沿AI技术，构建高等教育个性化学习资源体系，开发智能学习智能体系统，切实满足学生的个性化、多模态学习需求。

### 1.2 系统目标

- 通过自然语言对话构建学生动态学习画像（不少于6个维度）
- 多智能体协同生成多模态学习资源（教学文档、思维导图、题库、代码示例、拓展阅读）
- 基于学生画像生成个性化学习路径并推送资源
- 提供智能辅导答疑服务
- 实现学习效果评估与持续优化

### 1.3 技术栈

| 类别 | 技术/工具 | 说明 |
|------|----------|------|
| 开发语言 | Python 3.12 | 主开发语言 |
| 前端框架 | Streamlit | 快速构建交互式Web应用 |
| AI接口 | OpenAI兼容API | 支持科大讯飞等大模型接入 |
| 数据存储 | SQLite | 轻量级关系数据库 |
| 可视化 | Graphviz | 思维导图渲染 |
| 文档处理 | Markdown | 富文本内容渲染 |
| HTTP请求 | requests | API调用 |
| 配置管理 | python-dotenv | 环境变量加载（可选，内置降级解析器） |

---

## 2. 需求分析

### 2.1 功能需求分析

#### 2.1.1 对话式学习画像构建

**需求描述**：摒弃传统繁琐表单，支持通过自然语言对话自动抽取学生特征，构建包含不少于6个维度的动态学生画像。

**核心维度**：
- 专业：学生所属专业领域
- 知识水平：基础薄弱/中等/基础扎实
- 认知风格：视觉型/听觉型/动手型等
- 薄弱环节：易错点或薄弱知识点（列表）
- 学习目标：期望达成的学习目标（列表）
- 学习建议：针对性的学习建议（列表）
- 可选维度：兴趣方向、偏好资源类型、估计水平分数等

**设计要点**：
- 支持中文自然语言输入
- 自动提取结构化信息并转换为JSON格式
- 画像数据持久化存储
- 支持随学随新，动态更新画像

#### 2.1.2 多智能体协同的资源生成

**需求描述**：系统采用多智能体架构，不同角色的智能体协作生成至少5种类型的个性化资源。

**资源类型**：
1. **教学文档**：详细的知识点讲解文档
2. **思维导图**：知识点树状结构可视化（PNG格式）
3. **题库**：含题目、答案、详细解析的练习题库
4. **代码示例**：针对编程相关知识点的实操代码
5. **拓展阅读**：课外参考资料推荐（含摘要、难度、推荐理由）

**设计要点**：
- 各资源类型由专属子智能体负责生成
- 生成内容结合课程知识库，确保准确性
- 结构化输出便于前端渲染展示
- 提供资源下载功能（PNG/JSON等）

#### 2.1.3 个性化学习路径规划和资源推送

**需求描述**：整合系统生成的个性化资源，结合大模型对学生专业、学习进度、知识掌握情况及学习偏好的深度分析，为学生规划科学、动态的个性化学习路径。

**设计要点**：
- 基于学生画像生成阶段式学习计划
- 明确学习步骤和顺序
- 结合生成的多模态资源推荐
- 路径数据持久化存储

#### 2.1.4 智能辅导

**需求描述**：当学生在学习过程中遇到问题时，系统提供即时、多模态的答疑解惑服务。

**设计要点**：
- 支持自然语言问题输入
- 提供详细文字解答
- 多轮对话上下文保持
- Markdown格式渲染支持代码、公式、列表

#### 2.1.5 学习效果评估

**需求描述**：通过实时跟踪学生的学习行为、练习测试情况、资源使用反馈等数据，实现对学生学习效果的多维度、精准评估。

**设计要点**：
- 评估结果用于优化资源推送策略
- 动态调整学习计划
- 形成学习方案的持续优化闭环

### 2.2 非功能性需求分析

#### 2.2.1 用户界面需求

- 界面美观大方、简洁明了
- 交互逻辑清晰，符合现代AI产品交互规范
- 支持流式输出、Markdown渲染
- 多模态内容卡片化展示
- 无明显功能与界面错误

#### 2.2.2 内容安全与防幻觉

- 完善的内容安全过滤机制
- 确保生成的学术内容无事实性错误
- 无敏感违规信息输出
- API错误时提供清晰的错误提示

#### 2.2.3 性能与响应

- 智能体核心功能的响应时间控制在合理范围内
- 多模态资源生成提供进度提示
- 避免长时间白屏等待
- 优化图片渲染尺寸与分辨率

---

## 3. 系统架构设计

### 3.1 总体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        Streamlit 前端                        │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐       │
│  │ 画像构建│  │资源生成 │  │学习路径 │  │智能辅导 │  ...  │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘       │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    MultiAgentScheduler                       │
│                                                             │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ ProfileAgent│  │ ResourceAgent │  │  PathAgent   │       │
│  └─────────────┘  └──────────────┘  └──────────────┘       │
│                                                             │
│  ┌──────────────┐  ┌────────────────┐                      │
│  │ TutoringAgent│  │EvaluationAgent │                      │
│  └──────────────┘  └────────────────┘                      │
└────────────────────────────┬────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│  BaseAIClient  │  │  KnowledgeBase │  │    Database    │
│  (OpenAI兼容)  │  │ (课程知识库)    │  │   (SQLite)     │
└────────────────┘  └────────────────┘  └────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│                    大模型服务（如讯飞星火）                   │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 模块职责说明

#### 3.2.1 MultiAgentScheduler（多智能体调度器）

**文件位置**：`src/agents/multi_agent_scheduler.py`

**核心职责**：
- 负责各智能体的统一管理与任务分发
- 提供统一的任务执行入口 `execute_task(task_name, payload)`
- 支持完整流程执行 `run_full_flow(prompt, topic)`
- 异常捕获与错误结果标记

**关键方法**：

```python
def execute_task(self, task_name: str, payload: Any = None) -> Any:
    """执行指定任务并返回结果
    task_name: profile / resource / path / tutoring
    """
```

#### 3.2.2 ProfileAgent（用户画像智能体）

**文件位置**：`src/agents/profile_agent.py`

**核心职责**：
- 根据自然语言输入提取学生特征
- 构造结构化JSON格式的学习画像
- 确保至少6个维度的画像信息

**处理流程**：
1. 接收用户自然语言描述
2. 构造严格指令，要求LLM返回中文JSON
3. 调用AI客户端生成画像
4. 解析JSON并返回（API错误时标记`_ai_error`）

#### 3.2.3 ResourceAgent（资源生成智能体）

**文件位置**：`src/agents/resource_agent.py`
**子智能体**：`src/agents/resource_subagents.py`

**核心职责**：
- 资源生成总控，拆分为多个子智能体
- 统一调用各子智能体并汇总结果

**子智能体分工**：

| 子智能体 | 资源类型 | 输出格式 |
|---------|---------|---------|
| DocumentAgent | 教学文档 | Markdown文本 |
| MindmapAgent | 思维导图 | JSON结构（title/children） |
| QuestionBankAgent | 练习题库 | JSON数组（q/a/explanation） |
| CodeAgent | 代码示例 | 结构化代码 |
| ReadingMaterialAgent | 拓展阅读 | JSON列表（含元数据） |

#### 3.2.4 PathAgent（学习路径智能体）

**文件位置**：`src/agents/path_agent.py`

**核心职责**：
- 基于学生画像生成个性化学习路径
- 输出阶段式学习计划文本
- 整合资源推荐与学习步骤

#### 3.2.5 TutoringAgent（辅导答疑智能体）

**文件位置**：`src/agents/tutoring_agent.py`

**核心职责**：
- 接收学生问题并调用LLM解答
- 支持Markdown格式的富文本回答
- 提供结构化answer字段便于前端渲染

#### 3.2.6 EvaluationAgent（学习评估智能体）

**文件位置**：`src/agents/evaluation_agent.py`

**核心职责**：
- 对生成的教学文档进行质量评估
- 为后续优化提供反馈依据

### 3.3 核心数据模块

#### 3.3.1 KnowledgeBase（课程知识库）

**文件位置**：`src/core/knowledge_base.py`

**核心职责**：
- 管理多门课程的结构化知识数据
- 提供章节、知识点、代码示例、练习题、阅读材料的查询接口
- 为AI生成提供上下文参考内容

**课程目录结构**：
```
data/course_kb/
├── advanced_mathematics/          # 高等数学课程
│   ├── course_manifest.json       # 课程清单
│   ├── ch01_function_limit.json   # 第1章：函数与极限
│   ├── ch02_derivative_differential.json
│   └── ...
└── data_structures/               # 数据结构课程
    ├── course_manifest.json
    ├── ch01_introduction.json
    └── ...
```

**课程清单格式**：
```json
{
  "course_name": "数据结构",
  "course_name_en": "Data Structures",
  "target_major": "计算机科学与技术",
  "semester": "大二上学期",
  "total_hours": 64,
  "chapters": [
    {"id": "ch01", "title": "绪论", "hours": 4, "file": "ch01_introduction.json", "knowledge_points": [...]}
  ]
}
```

**章节数据格式**：
- `summary`：章节内容摘要
- `key_points`：核心知识点列表（含name/content/difficulty）
- `code_examples`：代码示例（含title/code/explanation）
- `questions`：练习题（含q/a/explanation）
- `reading_materials`：拓展阅读材料

**关键方法**：
- `list_courses()`：列出所有可用课程
- `get_knowledge_base(course_id)`：获取指定课程知识库
- `list_chapters()`：获取章节列表
- `search_knowledge_point(keyword)`：搜索知识点
- `build_prompt_context(topic)`：为AI生成构建参考上下文

#### 3.3.2 Database（数据持久化）

**文件位置**：`src/core/database.py`

**核心职责**：
- SQLite数据库操作封装
- 学生画像、学习路径、生成资源的持久化存储

**数据表设计**：

**表1：student_profiles（学生画像）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键自增 |
| user_id | TEXT | 用户标识（唯一） |
| feat1-feat6 | REAL | 6个数值特征 |
| extra | TEXT | JSON格式扩展信息 |
| created_at | TIMESTAMP | 创建时间 |

**表2：learning_paths（学习路径）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键自增 |
| user_id | TEXT | 用户标识 |
| step | INTEGER | 学习步骤序号 |
| progress | REAL | 进度百分比 |
| details | TEXT | 路径详细内容 |
| updated_at | TIMESTAMP | 更新时间 |

**表3：resources（生成资源）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键自增 |
| user_id | TEXT | 用户标识 |
| resource_id | TEXT | 资源标识（唯一） |
| resource_type | TEXT | 资源类型 |
| content | TEXT | 资源内容 |
| metadata | TEXT | JSON格式元数据 |
| created_at | TIMESTAMP | 创建时间 |

**关键方法**：
- `create_tables()`：创建所有数据表
- `add_student_profile(user_id, features, extra)`：添加/更新画像
- `add_learning_step(user_id, step, progress, details)`：记录学习步骤
- `add_resource(user_id, resource_id, resource_type, content, metadata)`：保存资源

#### 3.3.3 ResourceGenerator（资源渲染器）

**文件位置**：`src/core/resource_generator.py`

**核心职责**：
- 将LLM生成的JSON结构渲染为可视化资源
- 当前支持思维导图PNG生成

**思维导图渲染流程**：
1. 接收JSON树结构 `{"title": "...", "children": [...]}`
2. 使用Graphviz的Digraph构建有向图
3. 设置横向布局（rankdir=LR）、圆角节点
4. 支持节点note字段换行显示
5. 渲染为PNG文件（可自定义尺寸、dpi）
6. 返回生成文件路径供前端展示和下载

---

## 4. AI客户端设计

### 4.1 接口抽象层

**文件位置**：`src/api/base.py`

定义`BaseAIClient`抽象基类，核心方法：
- `generate_text(prompt: str) -> str`：生成文本内容

### 4.2 OpenAI兼容客户端

**文件位置**：`src/api/openai_client.py`

**设计特点**：
- 兼容OpenAI API协议（支持科大讯飞等兼容服务）
- 从`config/.env`读取配置
- 自动处理API认证、网络超时、错误响应
- 失败时返回带 `[AI错误]` 前缀的文本，便于前端识别

**配置文件格式**（`config/.env`）：
```
OPENAI_API_URL=https://api.example.com/v1/chat/completions
OPENAI_API_KEY=your-api-key-here
OPENAI_MODEL=gpt-4
```

### 4.3 配置加载机制

**文件位置**：`src/api/ai_client.py`

**设计要点**：
- 优先使用`python-dotenv`库解析配置
- 降级方案：手动解析器（无需第三方依赖）
- API密钥脱敏显示（中间字符替换为`****`）
- 配置项注入`os.environ`供下游读取

---

## 5. 前端界面设计

### 5.1 技术选型：Streamlit

**选择理由**：
- Python生态原生支持，代码量少
- 内置组件丰富（文本输入、按钮、下拉、图片、下载等）
- 支持Markdown渲染和HTML样式定制
- `session_state`管理会话状态
- 热重载支持快速开发迭代

### 5.2 页面结构

**文件位置**：`src/frontend/app.py`

#### 5.2.1 侧边导航栏

```
┌────────────────────┐
│ 📚 个性化学习智能体 │
├────────────────────┤
│ 👤 画像构建        │ ← 选中态
│ 📦 多模态资源生成  │
│ 🗺️ 个性化学习路径  │
│ 💬 智能辅导        │
│ 📚 课程知识库      │
└────────────────────┘
```

**样式设计**：
- 渐变色背景（`#667eea → #764ba2`）
- 圆角边框、阴影效果
- Hover时X方向位移+阴影增强

#### 5.2.2 课程知识库页面（含课程选择器）

**页面布局**：

```
[顶部课程选择器]
  👇 在下方选择一门课程来浏览对应的知识库
  [▼ 数据结构 (Data Structures)    ↓]

[课程头部信息区]
  ┌──────────────────────────────────┐
  │ 📚 数据结构  Data Structures     │
  │ 已完整构建设计并录入系统的高校... │
  │ 🏫 目标专业：计算机科学与技术     │
  │    ⏱ 总学时：64 课时             │
  └──────────────────────────────────┘

[统计信息卡片]
  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
  │   9     │ │  48     │ │  27     │ │  54     │
  │教学章节 │ │ 知识点  │ │代码示例 │ │ 练习题  │
  └─────────┘ └─────────┘ └─────────┘ └─────────┘

[知识点搜索]
  🔍 搜索关键词：[______________]  [搜索]

[章节列表]
  ┌──────────────────────────────┐
  │ ▼ 第1章：绪论（4学时）        │
  │   知识点：基本概念、算法分析 │
  │   [点击展开详情]              │
  └──────────────────────────────┘
  ...
```

**课程选择机制**：
- 顶部显示可用课程下拉列表（来源于`list_courses()`）
- 选择后写入`st.session_state["current_course_id"]`
- 知识库加载时读取该状态值，确保所有页面使用同一课程

#### 5.2.3 画像构建页面

**核心交互**：
1. 用户输入自然语言描述（如"我是一名计算机专业大二学生，想学机器学习"）
2. 点击"发送"按钮触发画像生成
3. 显示加载动画（"正在生成画像..."）
4. 生成结果以卡片形式展示（结构化字段）
5. 自动保存到SQLite数据库

#### 5.2.4 资源生成页面

**交互流程**：
1. 自动填充当前选中课程名称
2. 用户输入知识点关键词（如"二叉树遍历"）
3. 选择资源类型（document/mindmap/question_bank/code/reading_material）
4. 点击"🚀 生成资源"
5. 根据资源类型差异化渲染：
   - **思维导图**：解析JSON结构 → 调用Graphviz渲染PNG → 页面预览 + 下载按钮
   - **题库**：解析题目列表 → 每题折叠显示答案与解析 → Markdown渲染
   - **拓展阅读**：按order排序 → 卡片式展示（标题/难度/摘要/推荐理由）→ JSON下载
   - **其他**：Markdown文本或JSON展示

#### 5.2.5 学习路径页面

- 前提检查：需先生成学生画像
- 基于画像调用PathAgent生成路径
- 展示阶段式学习计划文本
- 自动保存学习步骤到数据库

#### 5.2.6 智能辅导页面

- 类聊天界面：用户消息（右侧紫色）、AI回复（左侧灰色）
- 支持按回车键发送
- AI回复支持Markdown（标题/列表/代码块/引用）
- 统一字体大小避免标题过大
- 对话历史保存在`session_state`

### 5.3 样式设计规范

**渐变色方案**：

| 页面 | 起始色 | 结束色 |
|------|--------|--------|
| 画像构建 | #667eea | #764ba2 |
| 资源生成 | #f093fb | #f5576c |
| 学习路径 | #4facfe | #00f2fe |
| 智能辅导 | #43e97b | #38f9d7 |
| 课程知识库 | #667eea | #764ba2 |

**通用样式类**：
- `.card`：白色内容卡片（圆角+阴影）
- `.btn-primary`：紫色渐变主按钮（Hover位移）
- `.tag-easy/normal/hard`：难度标签（绿/橙/红色背景）
- `.kp-box/code-box/q-box`：知识点/代码/题目容器

---

## 6. 创新实践与用户体验优化

### 6.1 多课程知识库架构

**创新点**：
- 目录式多课程管理（`data/course_kb/<course_id>/`）
- 课程清单统一描述元数据
- 前端课程选择器动态加载
- 按课程ID缓存实例减少磁盘IO

**实现细节**：
- `list_courses()`扫描子目录并读取`course_manifest.json`
- `get_knowledge_base(course_id)`返回指定课程实例
- `_course_cache`字典缓存已加载实例

### 6.2 多智能体任务调度

**创新点**：
- 统一入口`execute_task(task_name, payload)`替代直接调用各Agent
- 前端代码无需关注各Agent的具体实现差异
- 支持未来扩展新任务类型（如"evaluate"）无需修改前端

### 6.3 结构化输出与可视化渲染

**创新点**：
- 统一要求LLM返回JSON格式
- 思维导图从JSON树 → Graphviz有向图 → PNG图片
- 前端针对不同资源类型定制渲染逻辑（非一刀切文本展示）
- 提供文件下载能力（PNG/JSON）

### 6.4 错误处理与用户提示

**创新点**：
- API层错误统一用 `[AI错误]` 前缀或 `{"_ai_error": True, ...}` 标记
- 前端识别错误标记后显示红色错误横幅而非空白结果
- 配置缺失时给出明确修复指引（"请编辑config/.env填入密钥"）

### 6.5 会话状态管理

**创新点**：
- `session_state`统一管理：`profile`/`chat_history`/`current_course_id`/`ai`/`scheduler`/`db`
- 避免重复创建AI客户端和数据库连接
- 页面切换时保持上下文（如已选课程、对话历史）

---

## 7. 数据流向与业务流程

### 7.1 学生画像构建流程

```
用户输入自然语言
      │
      ▼
侧边栏点击"发送"
      │
      ▼
ProfileAgent.run(prompt)
  ├─ 构造指令：要求返回中文JSON画像
  ├─ 调用 BaseAIClient.generate_text()
  └─ 解析JSON → dict
      │
      ▼
前端渲染画像卡片
      │
      ▼
Database.add_student_profile()
  ├─ user_id: 基于输入哈希
  ├─ features: feat1-feat6数值特征
  └─ extra: 完整画像JSON
```

### 7.2 资源生成流程（以思维导图为例）

```
用户输入知识点 → 选择"mindmap"
      │
      ▼
ResourceAgent.run(topic)
  └─ MindmapAgent.run(topic)
        └─ 返回 JSON 树结构
              {"title": "...", "children": [...]}
      │
      ▼
ResourceGenerator.render_mindmap()
  ├─ 创建 Digraph（横向布局）
  ├─ 递归添加节点与边
  └─ 渲染为 PNG 文件
      │
      ▼
前端 st.image() 预览
      │
      ▼
st.download_button() 提供下载
      │
      ▼
Database.add_resource() 持久化
```

### 7.3 学习路径生成流程

```
检查 session_state.profile 是否存在
      │
      ├─ 不存在 → 提示"请先生成画像"
      │
      ▼
PathAgent.run(profile)
  └─ 基于画像生成阶段式学习计划
      │
      ▼
前端展示计划文本
      │
      ▼
Database.add_learning_step() 保存
```

### 7.4 智能辅导对话流程

```
用户输入问题 → 按回车/点击发送
      │
      ▼
添加到 session_state.chat_history
      │
      ▼
TutoringAgent.run(question)
  └─ 返回文本或 {"answer": "..."}
      │
      ▼
解析为Markdown → HTML
      │
      ▼
添加到 chat_history → 重新渲染
```

---

## 8. 部署与运行说明

### 8.1 环境要求

- **操作系统**：Linux（推荐）/ Windows / macOS
- **Python版本**：3.10 或更高（推荐3.12）
- **系统依赖**：graphviz（用于思维导图渲染，需安装系统级包）

### 8.2 安装步骤

**步骤1：克隆项目**
```bash
cd /path/to/workspace
git clone <repository-url>
cd A3-main
```

**步骤2（可选）：创建虚拟环境**
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# 或 Windows: .venv\Scripts\activate
```

**步骤3：安装Python依赖**
```bash
pip install streamlit openai requests graphviz markdown python-dotenv
```

**步骤4：安装系统级graphviz**
```bash
# Ubuntu/Debian
sudo apt-get install graphviz

# CentOS/RHEL
sudo yum install graphviz

# macOS (Homebrew)
brew install graphviz

# Windows: 下载安装包 https://graphviz.org/download/
```

**步骤5：配置API密钥**

创建 `config/.env` 文件：
```
OPENAI_API_URL=https://your-api-endpoint/v1/chat/completions
OPENAI_API_KEY=sk-your-actual-key
OPENAI_MODEL=spark-v3.5  # 或其他模型名
```

### 8.3 启动应用

```bash
cd /home/hjj/桌面/A3-main
streamlit run src/frontend/app.py
```

启动后访问终端显示的本地URL（通常为 `http://localhost:8501`）。

### 8.4 验证知识库

项目已预置两门完整课程：
- **数据结构**（Data Structures）：9章，含线性表、树、图、排序等
- **高等数学**（Advanced Mathematics）：8章，含函数极限、导数、积分、微分方程等

可在"课程知识库"页面浏览验证，也可通过命令行快速检查：
```bash
python -m src.core.knowledge_base
```

---

## 9. 测试策略与方法

### 9.1 测试概述

本系统采用分层测试策略，覆盖从底层数据模块到前端交互的完整链路，确保功能正确性、稳定性与用户体验。详细的测试用例设计见《测试说明书》。

### 9.2 测试分层

| 测试层级 | 覆盖范围 | 测试工具 |
|---------|---------|---------|
| 单元测试 | KnowledgeBase、Database、ResourceGenerator核心方法 | Python unittest/pytest |
| 功能测试 | 5大功能模块的完整交互流程 | 手动测试 + Streamlit交互验证 |
| 集成测试 | AI客户端 ↔ 智能体 ↔ 前端端到端链路 | 端到端场景测试 |
| 数据测试 | 两门课程知识库的数据完整性与可解析性 | JSON结构校验 |
| UI测试 | 页面布局、样式一致性、响应式适配 | 浏览器多分辨率验证 |
| 异常测试 | 网络异常、API错误、数据缺失等场景 | 故障注入测试 |

### 9.3 核心测试场景

#### 9.3.1 单元测试

- **KnowledgeBase模块**：验证课程列表扫描、知识库实例化、章节数据完整性、知识点搜索、提示上下文构建、无效课程ID容错
- **Database模块**：验证表结构创建、学生画像增删查、特征维度校验、学习路径持久化、资源存储与查询
- **ResourceGenerator模块**：验证思维导图JSON渲染、超大节点文本截断

#### 9.3.2 功能测试

- **画像构建**：完整画像生成流程（6维度字段）、短输入测试、空输入处理
- **资源生成**：教学文档、思维导图（含PNG下载）、题库（含答案折叠）、拓展阅读（含卡片排序）、代码示例
- **学习路径**：基于画像的路径生成、无画像场景提示
- **智能辅导**：单轮问答、多轮对话上下文、对话历史保持
- **课程知识库**：课程切换、知识点搜索、章节详情展开

#### 9.3.3 异常处理验证

- API网络超时/密钥无效 → 显示红色错误横幅，不崩溃
- LLM返回非JSON格式 → 降级逻辑生效，展示原始文本
- 缺失章节文件/JSON损坏 → 显示提示信息，不影响其他章节
- Graphviz未安装 → 捕获异常，回退展示JSON结构

#### 9.3.4 非功能性验证

- **性能**：画像构建 < 15秒，单种资源生成 < 20秒，辅导问答 < 10秒
- **内容安全**：生成内容无事实性错误、无敏感违规信息
- **UI一致性**：渐变色头部、卡片样式、按钮样式、字体大小统一

### 9.4 测试执行

```bash
# 运行知识库模块自检
python -m src.core.knowledge_base

# 运行单元测试（如已配置pytest）
pytest tests/ -v

# 手动功能测试：启动应用后逐一验证
streamlit run src/frontend/app.py
```

---

## 10. 目录结构

```
A3-main/
├── src/
│   ├── agents/                      # 多智能体模块
│   │   ├── __init__.py
│   │   ├── multi_agent_scheduler.py # 调度器（核心）
│   │   ├── profile_agent.py         # 画像智能体
│   │   ├── resource_agent.py        # 资源智能体（总控）
│   │   ├── resource_subagents.py    # 5个子智能体
│   │   ├── path_agent.py            # 路径智能体
│   │   ├── tutoring_agent.py        # 辅导智能体
│   │   └── evaluation_agent.py      # 评估智能体
│   ├── api/                         # AI客户端
│   │   ├── ai_client.py             # 配置加载与客户端获取
│   │   ├── base.py                  # 客户端抽象基类
│   │   └── openai_client.py         # OpenAI兼容实现
│   ├── core/                        # 核心数据模块
│   │   ├── database.py              # SQLite封装
│   │   ├── knowledge_base.py        # 课程知识库管理
│   │   └── resource_generator.py    # 思维导图渲染器
│   └── frontend/
│       └── app.py                   # Streamlit主应用
├── data/
│   ├── course_kb/                   # 课程知识库数据
│   │   ├── data_structures/         # 数据结构课程
│   │   └── advanced_mathematics/    # 高等数学课程
│   ├── generated/                   # 生成文件存储目录
│   └── user_profiles/               # 用户画像数据库目录
├── config/
│   └── .env                         # API配置（需自行创建）
├── 系统开发说明书.md                # 本文档
└── 测试说明书.md                    # 测试文档
```

---

## 11. 关键技术实现细节

### 11.1 JSON安全解析

**问题**：LLM可能在JSON前后添加解释性文本或代码块标记

**解决方案**：
- `ResourceGenerator._extract_json()`：用正则 `r"(\{.*\})"` 提取最外层JSON块
- ProfileAgent：优先尝试`json.loads()`，失败则返回原始文本供前端调试

### 11.2 Graphviz中文支持

**问题**：默认Graphviz渲染中文可能出现乱码

**解决方案**：
- 确保系统安装中文字体（如Noto CJK、WenQuanYi）
- Python源文件使用UTF-8编码
- 节点文本先做换行处理避免超宽

### 11.3 Streamlit会话保持

**问题**：每次交互触发脚本重跑，需避免重复创建对象

**解决方案**：
- 所有长生命周期对象（AI Client、调度器、数据库连接）存入`session_state`
- 首次访问时初始化，后续复用
- `st.rerun()`强制刷新用于更新UI状态

### 11.4 数据库并发访问

**问题**：Streamlit多会话可能并发写入SQLite

**解决方案**：
- SQLite文件锁机制天然支持并发读取
- 写入操作通过`INSERT OR REPLACE`实现幂等
- `timeout=30`秒超时避免锁等待失败

---

## 12. 扩展与优化方向

| 方向 | 说明 |
|------|------|
| 异步生成 | 当前为同步调用，可改造为异步流式输出 |
| 更多资源类型 | 如PPT生成、短视频生成、语音讲解 |
| 学习进度追踪 | 记录用户点击、停留、练习完成度 |
| 画像动态更新 | 随用户学习行为自动更新画像特征 |
| 多用户系统 | 引入登录鉴权，数据按用户隔离 |
| 缓存机制 | LLM调用结果缓存，减少重复请求 |
| 测试覆盖 | 添加单元测试与集成测试 |

---

*文档版本：v1.0*  
*最后更新：2026年6月*