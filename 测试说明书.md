# 个性化学习资源智能体系统 - 测试说明书

## 1. 测试概述

### 1.1 测试目标

本测试说明书旨在对"个性化学习资源智能体系统"进行全面的质量验证，确保系统在功能正确性、稳定性、用户体验、内容安全性等方面达到预期标准。测试覆盖从底层数据模块到前端交互的完整链路。

### 1.2 测试范围

| 测试类别 | 覆盖范围 |
|---------|---------|
| 单元测试 | KnowledgeBase、Database、ResourceGenerator核心方法 |
| 功能测试 | 5大功能模块的完整交互流程 |
| 集成测试 | AI客户端 ↔ 智能体 ↔ 前端的端到端链路 |
| 数据测试 | 两门课程知识库的数据完整性与可解析性 |
| UI测试 | 页面布局、样式一致性、响应式适配 |
| 异常测试 | 网络异常、API错误、数据缺失等场景 |

### 1.3 测试环境

| 项目 | 规格 |
|------|------|
| 操作系统 | Ubuntu 22.04 LTS / Windows 11 |
| Python版本 | 3.12 |
| 浏览器 | Chrome 120+ / Firefox 120+ / Edge 120+ |
| 网络 | 可访问配置的AI API端点 |
| 数据库 | SQLite 3.x（随Python） |
| 依赖包 | streamlit、openai、requests、graphviz、markdown、python-dotenv |

---

## 2. 单元测试用例

### 2.1 KnowledgeBase 模块测试

**文件**：`src/core/knowledge_base.py`

#### TC-UB-001：课程列表扫描

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证能否正确扫描并列出所有课程 |
| 前置条件 | `data/course_kb/`下存在至少一个课程子目录 |
| 测试步骤 | 调用 `list_courses()` |
| 预期结果 | 1. 返回列表非空；2. 每项包含`id/course_name/course_name_en`；3. 至少包含"data_structures"和"advanced_mathematics"两门课程 |
| 验证代码 | `python -c "from src.core.knowledge_base import list_courses; cs=list_courses(); [print(c['id'], c['course_name']) for c in cs]; assert len(cs)>=2"` |

#### TC-UB-002：知识库实例化

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证能否正确加载指定课程的知识库 |
| 测试步骤 | 分别调用 `get_knowledge_base("data_structures")` 和 `get_knowledge_base("advanced_mathematics")` |
| 预期结果 | 1. 实例创建成功；2. `course_name`字段正确；3. `list_chapters()`返回非空章节列表 |

#### TC-UB-003：章节数据完整性

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证每章JSON数据是否包含必要字段 |
| 测试步骤 | 对每章调用`get_chapter()`，检查是否包含：summary、key_points、code_examples、questions、reading_materials |
| 预期结果 | 所有章节数据结构完整，无KeyError |

#### TC-UB-004：知识点搜索

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证关键词搜索功能 |
| 测试数据 | 关键词："链表"（数据结构）、"导数"（高等数学） |
| 测试步骤 | 调用 `search_knowledge_point("链表")` |
| 预期结果 | 返回匹配列表，每项含chapter_id/chapter_title/kp_name/content |

#### TC-UB-005：提示上下文构建

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证构建给LLM的参考上下文 |
| 测试步骤 | 调用 `build_prompt_context("排序算法")` |
| 预期结果 | 返回文本包含课程名、相关知识点摘要；长度在合理范围内（不超过max_chars限制） |

#### TC-UB-006：无效课程ID容错

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证对不存在课程ID的容错处理 |
| 测试步骤 | 调用 `get_knowledge_base("non_existent_course")` |
| 预期结果 | 不抛出异常；返回默认课程实例或优雅降级 |

---

### 2.2 Database 模块测试

**文件**：`src/core/database.py`

#### TC-UB-101：表结构创建

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证三张数据表是否正确创建 |
| 测试步骤 | 1. 实例化Database（指定临时路径）；2. 调用`create_tables()`；3. 查询SQLite `sqlite_master` |
| 预期结果 | student_profiles/learning_paths/resources三张表均存在 |

#### TC-UB-102：学生画像增删查

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. `add_student_profile("test_user_001", [0.8, 0.6, 0.9, 0.7, 0.5, 0.8], {"major": "计算机"})`；2. `get_student_profile("test_user_001")`；3. `delete_student_profile("test_user_001")` |
| 预期结果 | 1. 添加成功返回id；2. 查询结果feat1-feat6匹配，extra字段可反序列化为dict；3. 删除后再次查询返回None |

#### TC-UB-103：特征维度验证

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证features必须为6个数值的约束 |
| 测试步骤 | 传入5个或7个数值调用`add_student_profile` |
| 预期结果 | 抛出ValueError异常 |

#### TC-UB-104：学习路径持久化

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. `add_learning_step("user_p001", 1, 0.0, "第1阶段：基础概念学习")`；2. `add_learning_step("user_p001", 2, 0.0, "第2阶段：算法实践")`；3. `get_learning_path("user_p001")` |
| 预期结果 | 返回按step排序的两条记录，内容与写入一致 |

#### TC-UB-105：资源存储与查询

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. `add_resource("user_r001", "res_bst_01", "mindmap", "<binary or text>", {"topic": "二叉搜索树"})`；2. `get_resource("res_bst_01")`；3. `list_resources("user_r001")` |
| 预期结果 | 1. 添加成功；2. 查询返回完整元数据；3. 列表包含刚添加的资源 |

---

### 2.3 ResourceGenerator 模块测试

**文件**：`src/core/resource_generator.py`

#### TC-UB-201：思维导图JSON渲染

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证能否从JSON结构正确渲染PNG |
| 测试输入 | `{"title": "测试", "children": [{"title": "子节点1"}, {"title": "子节点2", "note": "这是注释"}]}` |
| 测试步骤 | `ResourceGenerator(work_dir="/tmp/test_gen").render_mindmap(structure, output_name="test_mindmap")` |
| 预期结果 | 1. 无异常抛出；2. 指定路径生成PNG文件；3. 文件大小>0 |

#### TC-UB-202：超大节点文本截断

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证超长文本是否被合理截断避免渲染异常 |
| 测试输入 | 构造一个title字段超过300字符的节点 |
| 预期结果 | 成功渲染，无Graphviz异常；节点文本被截断至合理长度 |

---

### 2.4 AI客户端测试

#### TC-UB-301：配置文件读取

| 项目 | 内容 |
|------|------|
| 测试步骤 | 调用 `get_ai_client()` |
| 预期结果 | 1. 控制台输出读取到的配置项数量；2. API密钥中间部分被脱敏为`****`；3. OPENAI_API_URL/OPENAI_MODEL正确显示 |

#### TC-UB-302：缺少API密钥场景

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证无密钥时客户端行为 |
| 测试步骤 | 临时移除/重命名`.env`后调用`generate_text()` |
| 预期结果 | 1. 输出警告信息；2. 返回以`[AI错误]`开头的错误文本；3. 不抛出未捕获异常 |

---

## 3. 功能测试用例

### 3.1 画像构建功能测试

#### TC-FN-001：完整画像生成流程

| 项目 | 内容 |
|------|------|
| 测试页面 | 👤 画像构建 |
| 测试输入 | "我是计算机专业大二学生，目前正在学习数据结构，感觉链表和树的部分理解不够深入，希望加强算法设计能力，平时喜欢看视频教程和做编程练习" |
| 测试步骤 | 1. 输入文本 → 2. 点击"发送" → 3. 等待生成 → 4. 检查结果卡片 |
| 预期结果 | 1. 显示加载动画；2. 生成结构化画像卡片；3. 包含至少6个字段（专业、知识水平、认知风格、薄弱环节、学习目标、学习建议）；4. 字段值为中文且合理；5. 控制台显示数据库保存成功 |

#### TC-FN-002：短输入测试

| 项目 | 内容 |
|------|------|
| 测试输入 | "想学机器学习" |
| 预期结果 | 仍能生成合理画像（可能某些字段为默认值），不报错 |

#### TC-FN-003：空输入处理

| 项目 | 内容 |
|------|------|
| 测试步骤 | 不输入任何内容直接点击发送 |
| 预期结果 | 显示提示"请输入学生信息后再发送"（黄色警告），不触发API调用 |

---

### 3.2 资源生成功能测试

#### TC-FN-101：教学文档生成

| 项目 | 内容 |
|------|------|
| 测试页面 | 📦 多模态资源生成 |
| 测试参数 | 课程：数据结构；知识点："快速排序"；类型：document |
| 预期结果 | 1. 生成Markdown格式的讲解文档；2. 包含算法原理、步骤说明、复杂度分析等内容；3. 文本格式正确可渲染 |

#### TC-FN-102：思维导图生成与下载

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程：高等数学；知识点："微分中值定理"；类型：mindmap |
| 测试步骤 | 1. 生成后预览PNG图像；2. 点击下载按钮保存文件 |
| 预期结果 | 1. PNG图像正常显示（横向布局、树状结构）；2. 下载的文件与页面预览一致；3. 文件名包含知识点关键词 |

#### TC-FN-103：题库结构化渲染

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程：数据结构；知识点："二叉树遍历"；类型：question_bank |
| 预期结果 | 1. 展示多道题目；2. 每题默认隐藏答案与解析；3. 点击"查看答案与解析"展开后显示答案（a字段）和详解（explanation字段）；4. Markdown格式正确 |

#### TC-FN-104：拓展阅读推荐

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程：高等数学；知识点："定积分"；类型：reading_material |
| 预期结果 | 1. 展示多条推荐；2. 每条含标题、类型、难度、预计耗时、摘要、推荐理由等；3. 按order字段升序排列；4. 提供JSON下载按钮 |

#### TC-FN-105：代码示例生成

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程：数据结构；知识点："哈希表"；类型：code |
| 预期结果 | 1. 返回可运行的代码示例；2. 包含代码注释说明；3. 代码块格式正确渲染 |

#### TC-FN-106：缺少输入参数

| 项目 | 内容 |
|------|------|
| 测试步骤 | 不填写课程或知识点直接生成 |
| 预期结果 | 显示警告"请填写课程和知识点以生成资源"，不触发API |

---

### 3.3 学习路径功能测试

#### TC-FN-201：基于画像的路径生成

| 项目 | 内容 |
|------|------|
| 测试页面 | 🗺️ 个性化学习路径 |
| 前置条件 | 已在"画像构建"页面生成并保存了学生画像 |
| 预期结果 | 1. 无警告提示；2. 生成阶段式学习路径文本；3. 包含基础巩固→核心学习→综合提升等阶段；4. 数据库保存learning_path记录 |

#### TC-FN-202：无画像场景

| 项目 | 内容 |
|------|------|
| 前置条件 | session_state中无profile（清除会话或新开浏览器） |
| 测试步骤 | 直接进入学习路径页面点击生成 |
| 预期结果 | 显示橙色提示"请先在左侧的'画像构建'页面生成或输入学生画像" |

---

### 3.4 智能辅导功能测试

#### TC-FN-301：单轮问答

| 项目 | 内容 |
|------|------|
| 测试页面 | 💬 智能辅导 |
| 测试问题 | "什么是时间复杂度O(n log n)？请举例说明" |
| 预期结果 | 1. 用户消息显示在右侧紫色气泡；2. AI在合理时间内返回答案；3. AI消息显示在左侧灰色气泡；4. Markdown格式（如代码、列表）正确渲染 |

#### TC-FN-302：多轮对话上下文

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 问"什么是二叉树？"；2. 再问"它有哪些常见的遍历方式？" |
| 预期结果 | 第二个问题的回答能结合上下文，不会重复定义二叉树，直接回答遍历方式 |

#### TC-FN-303：对话历史保持

| 项目 | 内容 |
|------|------|
| 测试步骤 | 进行3-5轮对话后，切换到其他页面再返回智能辅导 |
| 预期结果 | 之前的对话历史仍在chat_history中，页面完整渲染所有历史消息 |

---

### 3.5 课程知识库功能测试

#### TC-FN-401：课程切换

| 项目 | 内容 |
|------|------|
| 测试页面 | 📚 课程知识库 |
| 测试步骤 | 1. 在顶部下拉选择"数据结构"；2. 检查页面头部和统计数据；3. 切换选择"高等数学"；4. 再次检查 |
| 预期结果 | 1. 切换后课程名、英文名、目标专业、总学时正确更新；2. 章节数、知识点数、代码示例数、练习题数正确变化；3. 章节列表内容随课程变化 |

#### TC-FN-402：知识点搜索

| 项目 | 内容 |
|------|------|
| 测试步骤 | 在当前课程的搜索框输入关键词，如"时间复杂度" |
| 预期结果 | 1. 高亮显示匹配的章节；2. 展开匹配章节时显示命中的知识点内容 |

#### TC-FN-403：章节详情展开

| 项目 | 内容 |
|------|------|
| 测试步骤 | 点击任意章节卡片展开 |
| 预期结果 | 显示：1. 章节摘要；2. 知识点列表（带难度标签）；3. 代码示例（代码框）；4. 练习题；5. 拓展阅读 |

---

## 4. 集成测试用例

### 4.1 端到端流程测试

#### TC-INT-001：完整学习场景

| 项目 | 内容 |
|------|------|
| 场景描述 | 模拟一个学生从构建画像到获取资源、规划路径、提问辅导的完整学习体验 |
| 测试步骤 | |
| 步骤1 | 在"画像构建"页面输入学生描述，生成画像 |
| 步骤2 | 切换到"课程知识库"，选择目标课程（如数据结构），浏览章节了解课程结构 |
| 步骤3 | 切换到"多模态资源生成"，针对薄弱知识点（如"二叉树"）生成：教学文档 + 思维导图 + 题库 |
| 步骤4 | 在"个性化学习路径"页面基于画像生成学习计划 |
| 步骤5 | 在"智能辅导"页面提问2-3个相关问题 |
| 验收标准 | 1. 全流程无崩溃/无未捕获异常；2. 每个页面输出内容合理且格式正确；3. PNG文件可下载且正常；4. 数据库中新增相应的profile/path/resource记录 |

---

### 4.2 多课程切换测试

#### TC-INT-101：课程切换后资源生成一致性

| 项目 | 内容 |
|------|------|
| 测试步骤 | 1. 在知识库选择"数据结构"；2. 去资源生成页面确认课程自动填充为"数据结构"；3. 生成一个资源；4. 返回知识库切换到"高等数学"；5. 再次去资源生成页面 |
| 预期结果 | 课程名称跟随知识库选择动态更新，生成的资源与选中课程主题相关 |

---

## 5. 异常与错误处理测试

### 5.1 API异常测试

#### TC-ERR-001：API网络超时

| 项目 | 内容 |
|------|------|
| 模拟方法 | 暂时断开网络连接或将API URL配置为无效地址 |
| 测试步骤 | 触发任意需要调用AI的功能（画像生成/资源生成/辅导问答） |
| 预期结果 | 1. 不崩溃；2. 显示红色错误横幅"AI调用失败：..."；3. 包含具体错误信息（如"连接超时"）便于调试；4. 页面其他功能仍可用 |

#### TC-ERR-002：API密钥无效

| 项目 | 内容 |
|------|------|
| 模拟方法 | 在`.env`中填入无效API密钥 |
| 预期结果 | 显示认证失败错误信息，不返回伪造内容 |

#### TC-ERR-003：返回非JSON格式

| 项目 | 内容 |
|------|------|
| 场景 | LLM偶尔返回带解释文本的非纯JSON |
| 预期结果 | 1. ProfileAgent的`_extract_json`/降级逻辑生效；2. 展示原始文本便于调试；3. 不抛出JSON解析异常导致页面崩溃 |

---

### 5.2 数据异常测试

#### TC-ERR-101：缺失章节文件

| 项目 | 内容 |
|------|------|
| 模拟方法 | 临时重命名某个章节JSON文件 |
| 预期结果 | 1. 课程清单仍能列出该章节（基于manifest）；2. 点击展开时显示该章无详细数据的提示；3. 不影响其他章节正常显示 |

#### TC-ERR-102：JSON格式损坏

| 项目 | 内容 |
|------|------|
| 模拟方法 | 故意破坏某个章节JSON的语法（如删除一个括号） |
| 预期结果 | 1. JSON解析失败不抛出未捕获异常；2. 该章显示加载错误提示；3. 其他章节和课程功能正常 |

---

### 5.3 资源渲染异常测试

#### TC-ERR-201：Graphviz未安装

| 项目 | 内容 |
|------|------|
| 模拟方法 | 在未安装系统级graphviz的环境生成思维导图 |
| 预期结果 | 1. 捕获graphviz异常；2. 显示"思维导图渲染失败"的错误消息；3. 回退展示JSON结构供人工阅读 |

---

## 6. 非功能性测试

### 6.1 性能测试

#### TC-NF-001：API响应时间

| 项目 | 内容 |
|------|------|
| 测试目标 | 各智能体功能的响应时间在可接受范围内 |
| 测试方法 | 记录3-5次调用的平均耗时 |
| 验收标准 | 画像构建 < 15秒；单种资源生成 < 20秒；辅导问答 < 10秒 |

#### TC-NF-002：知识库加载性能

| 项目 | 内容 |
|------|------|
| 测试目标 | 首次加载课程知识库耗时 < 2秒 |
| 测试方法 | `time python -c "from src.core.knowledge_base import get_knowledge_base; kb=get_knowledge_base('data_structures'); print(len(kb.list_chapters()), '章')"` |

#### TC-NF-003：思维导图渲染大小

| 项目 | 内容 |
|------|------|
| 测试目标 | PNG文件尺寸合理，不超过5MB |
| 验收标准 | 常规知识点思维导图 < 500KB |

---

### 6.2 用户体验测试

#### TC-NF-101：UI样式一致性

| 检查项 | 标准 |
|--------|------|
| 渐变色头部 | 每个功能页面有统一风格的渐变色标题区 |
| 卡片样式 | 内容卡片均使用圆角+阴影，边距一致 |
| 按钮样式 | 主按钮统一使用紫色渐变，Hover有位移效果 |
| 字体大小 | 正文14-16px，标题24-28px，无超大标题 |
| 标签样式 | 难度标签使用绿/橙/红三色区分 |

#### TC-NF-102：加载反馈

| 检查项 | 标准 |
|--------|------|
| AI调用期间 | 显示spinner动画和提示文字（如"正在生成画像..."） |
| 无输入状态 | 清晰提示用户需要做什么（如"请输入学生信息"） |
| 长操作期间 | 页面保持响应，无白屏卡死 |

#### TC-NF-103：响应式布局

| 检查项 | 标准 |
|--------|------|
| 桌面端（1920x1080） | 内容居中，两侧留白合理 |
| 中等屏幕（1366x768） | 布局正常不溢出 |
| 移动端（可选） | 侧栏自动折叠，内容单列显示 |

---

### 6.3 内容安全与防幻觉测试

#### TC-NF-201：敏感词过滤（可选增强项）

| 测试方法 | 尝试输入包含敏感内容的提示词 |
|----------|-------------------------|
| 预期结果 | 系统给出合规提示或生成安全内容 |

#### TC-NF-202：学术准确性校验

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证生成的知识点内容无明显事实错误 |
| 测试方法 | 生成"时间复杂度"的教学文档，人工核对基本概念是否准确 |
| 验收标准 | 1. 概念定义正确；2. 示例代码逻辑合理；3. 复杂度分析无误 |

---

## 7. 课程知识库数据测试

### 7.1 数据结构课程完整性

#### TC-DS-001：数据结构章节清单

| 验证项 | 预期内容 |
|--------|---------|
| 总章节数 | 9章 + 1补充 |
| 章节1 | 绪论：基本概念、算法分析、复杂度 |
| 章节2 | 线性表：顺序表、链表、双向链表、循环链表 |
| 章节3 | 栈和队列：栈、队列、双端队列、优先级队列 |
| 章节4 | 串和数组：字符串匹配、多维数组、广义表 |
| 章节5 | 树和二叉树：二叉树、树、森林、哈夫曼树 |
| 章节6 | 图：图存储、遍历、最短路径、最小生成树 |
| 章节7 | 查找：顺序查找、二分查找、哈希表、二叉搜索树 |
| 章节8 | 排序：冒泡、选择、插入、快排、归并、堆排序 |
| 章节9 | 文件：外存、文件组织、索引、B树 |

#### TC-DS-002：代码示例可运行性（抽样检查）

从各章节抽取1-2个代码示例：
1. 线性表：链表插入/删除
2. 树：二叉树前序/中序/后序遍历
3. 排序：快速排序实现
4. 图：BFS/DFS遍历

验证方法：复制代码到Python解释器执行，确认无语法错误且逻辑正确。

---

### 7.2 高等数学课程完整性

#### TC-DS-101：高等数学章节清单

| 验证项 | 预期内容 |
|--------|---------|
| 总章节数 | 8章 |
| 章节1 | 函数与极限：函数概念、极限定义、极限运算、无穷小、连续性 |
| 章节2 | 导数与微分：导数定义、求导法则、高阶导数、微分、隐函数求导 |
| 章节3 | 微分中值定理与导数应用：罗尔定理、拉格朗日、柯西、洛必达、泰勒、单调性、极值、凹凸性 |
| 章节4 | 不定积分：原函数、积分公式、换元法、分部积分、有理函数积分 |
| 章节5 | 定积分及其应用：定积分定义、牛顿莱布尼茨公式、换元/分部、面积、体积、弧长、物理应用 |
| 章节6 | 微分方程：基本概念、一阶微分方程、可分离变量、齐次、线性、伯努利、二阶常系数 |
| 章节7 | 多元函数微分学：多元函数、偏导数、全微分、复合函数求导、方向导数、梯度、极值、条件极值 |
| 章节8 | 多元函数积分学：二重积分、三重积分、累次积分、极坐标、应用 |

#### TC-DS-102：数学公式与例题质量

抽样检查每章的：
- 核心定理表述是否准确
- 例题是否有完整解答步骤
- 公式符号使用是否规范

---

## 8. 兼容性测试

### 8.1 浏览器兼容性

| 浏览器 | 版本 | 画像构建 | 资源生成 | 思维导图PNG | 智能辅导 | 知识库浏览 |
|--------|------|---------|---------|------------|---------|-----------|
| Chrome | 120+ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Firefox | 120+ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Edge | 120+ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Safari | 17+ | ✓ | ✓ | ✓ | ✓ | ✓ |

### 8.2 操作系统兼容性

| 操作系统 | Python版本 | Streamlit运行 | Graphviz渲染 | 备注 |
|---------|-----------|--------------|-------------|------|
| Ubuntu 22.04 | 3.12 | ✓ | ✓ | 推荐开发环境 |
| Windows 11 | 3.12 | ✓ | ✓ | 需额外安装graphviz |
| macOS 14 | 3.12 | ✓ | ✓ | brew install graphviz |

---

## 9. 验收测试清单

### 9.1 必测功能（核心）

- [ ] TC-FN-001 完整画像生成（≥6维度）
- [ ] TC-FN-102 思维导图生成+PNG下载
- [ ] TC-FN-103 题库结构化展示
- [ ] TC-FN-104 拓展阅读推荐卡片
- [ ] TC-FN-201 基于画像的学习路径
- [ ] TC-FN-301 智能辅导单轮问答
- [ ] TC-FN-401 课程切换（数据结构↔高数）
- [ ] TC-INT-001 完整学习场景端到端

### 9.2 异常处理（健壮性）

- [ ] TC-ERR-001 API网络超时
- [ ] TC-ERR-002 API密钥无效
- [ ] TC-ERR-101 缺失章节文件
- [ ] TC-ERR-201 Graphviz未安装

### 9.3 数据完整性

- [ ] TC-DS-001 数据结构9章节清单
- [ ] TC-DS-101 高等数学8章节清单
- [ ] TC-DS-002 代码示例抽样运行测试

### 9.4 性能指标

- [ ] TC-NF-001 API响应时间 ≤ 20秒
- [ ] TC-NF-002 知识库加载 ≤ 2秒
- [ ] TC-NF-003 PNG大小 ≤ 500KB

---

## 10. 测试执行记录模板

### 测试执行记录表

| 用例编号 | 用例名称 | 测试日期 | 测试人员 | 测试环境 | 实际结果 | 耗时 | 状态 | 问题编号 |
|---------|---------|---------|---------|---------|---------|-----|------|---------|
| TC-UB-001 | 课程列表扫描 | | | | | | | |
| TC-UB-101 | 表结构创建 | | | | | | | |
| TC-FN-001 | 完整画像生成 | | | | | | | |
| TC-FN-102 | 思维导图生成 | | | | | | | |
| TC-INT-001 | 完整学习场景 | | | | | | | |
| ... | | | | | | | | |

### 问题登记模板

| 问题编号 | 严重程度 | 问题描述 | 复现步骤 | 影响模块 | 修复状态 | 修复日期 | 修复人 |
|---------|---------|---------|---------|---------|---------|---------|-------|
| BUG-001 | 高/中/低 | | | | 待修复/已修复 | | |
| BUG-002 | | | | | | | |

**严重程度定义**：
- **高**：系统崩溃、核心功能不可用、数据丢失
- **中**：功能异常但有替代方案、UI显示错乱
- **低**：轻微UI问题、文案优化、性能微小波动

---

## 11. 测试工具与辅助脚本

### 11.1 快速验证脚本（命令行）

**脚本1：知识库数据完整性检查**
```bash
cd /home/hjj/桌面/A3-main
python -c "
from src.core.knowledge_base import list_courses, get_knowledge_base
courses = list_courses()
print(f'发现 {len(courses)} 门课程:')
for c in courses:
    print(f'  - {c[\"id\"]}: {c[\"course_name\"]} ({c[\"course_name_en\"]})')
    kb = get_knowledge_base(c['id'])
    chapters = kb.list_chapters()
    print(f'    章节数: {len(chapters)}')
    total_kp = sum(len(kb.get_chapter(ch['id']).get('key_points', [])) for ch in chapters if kb.get_chapter(ch['id']))
    total_code = sum(len(kb.get_chapter(ch['id']).get('code_examples', [])) for ch in chapters if kb.get_chapter(ch['id']))
    total_q = sum(len(kb.get_chapter(ch['id']).get('questions', [])) for ch in chapters if kb.get_chapter(ch['id']))
    print(f'    知识点: {total_kp}, 代码: {total_code}, 练习: {total_q}')
    # 抽样验证JSON可解析性
    for ch in chapters[:2]:
        data = kb.get_chapter(ch['id'])
        assert data, f'章节{ch[\"id\"]}数据为空'
    print(f'    抽样JSON解析: OK')
"
```

**脚本2：数据库CRUD测试**
```bash
python -c "
import tempfile, os
from src.core.database import Database
db_path = tempfile.mktemp(suffix='.db')
db = Database(db_path=db_path)
db.create_tables()
# 增
pid = db.add_student_profile('u1', [1,2,3,4,5,6], {'x': 'test'})
print(f'添加画像, id={pid}')
# 查
p = db.get_student_profile('u1')
assert p and p['feat1'] == 1
print(f'查询画像: {p[\"extra\"]}')
# 增路径
db.add_learning_step('u1', 1, 0.0, 'Step 1')
db.add_learning_step('u1', 2, 0.0, 'Step 2')
path = db.get_learning_path('u1')
assert len(path) == 2
print(f'查询路径: {len(path)} 步')
# 增资源
db.add_resource('u1', 'r1', 'mindmap', 'content...', {'topic': 'tree'})
r = db.get_resource('r1')
assert r and r['resource_type'] == 'mindmap'
print(f'查询资源: {r[\"resource_id\"]}')
# 清理
os.unlink(db_path)
print('所有CRUD操作: PASS ✓')
"
```

**脚本3：思维导图渲染测试**
```bash
python -c "
import json, os, tempfile
from src.core.resource_generator import ResourceGenerator
rg = ResourceGenerator(work_dir=tempfile.gettempdir())
struct = {'title': '测试知识点树', 'children': [
    {'title': '基础概念', 'children': [
        {'title': '定义', 'note': '这是核心定义'},
        {'title': '性质'},
    ]},
    {'title': '应用场景', 'children': [
        {'title': '场景1'},
        {'title': '场景2'},
    ]}
]}
out = rg.render_mindmap(struct, output_name='unittest_mindmap', size='8,5', dpi=150)
print(f'生成文件: {out}')
print(f'文件大小: {os.path.getsize(out)} bytes')
assert os.path.getsize(out) > 1000
print('渲染测试: PASS ✓')
os.unlink(out)
"
```

**脚本4：启动应用冒烟测试**
```bash
cd /home/hjj/桌面/A3-main
# 检查应用是否能正常启动（不会挂掉）
timeout 10 streamlit run src/frontend/app.py --server.headless true 2>&1 | head -30
# 预期输出包含 "You can now view your Streamlit app in your browser."
```

### 11.2 手动测试检查清单

```
□ 浏览器打开应用首页加载正常
□ 侧栏5个页面按钮点击响应正常
□ 课程知识库页面顶部下拉能切换两门课程
□ 画像构建：输入文本生成后显示6+字段
□ 资源生成：5种类型各生成一个，均有合理输出
□ 思维导图：PNG预览正常，下载按钮可用
□ 题库：题目列表显示正常，点击展开显示答案
□ 学习路径：基于画像生成计划文本
□ 智能辅导：3-5轮对话历史保持
□ 数据库：检查 profiles.db 是否生成且非空
```

---

## 12. 常见问题与排错指南

### Q1：启动后所有AI功能都返回 "[AI错误]"

**排查**：
1. 检查 `config/.env` 是否存在且包含正确的API Key
2. 检查API URL是否可访问（`curl -i <API_URL>`）
3. 检查密钥格式是否正确（无多余空格/引号）
4. 查看控制台输出的配置项是否正确显示

### Q2：思维导图生成报错 "failed to execute PosixPath('dot')"

**原因**：系统级graphviz未安装  
**解决**：
```bash
# Ubuntu/Debian
sudo apt-get install graphviz
# 验证
which dot && dot -V
```

### Q3：切换课程后资源生成页面的课程名未更新

**排查**：
1. 检查 `st.session_state["current_course_id"]` 是否正确设置
2. 确认资源生成页面调用 `get_kb_for_current_course()` 时使用了最新值
3. 可能需要切换页面或触发重新渲染

### Q4：数据库文件找不到 / 权限问题

**默认路径**：`data/user_profiles/profiles.db`  
**排查**：
1. 确认目录存在且有写权限
2. 检查是否被其他进程锁定（`lsof profiles.db`）

### Q5：Streamlit启动很慢 / 首次加载卡顿

**优化建议**：
1. 启用Python缓存编译（`.pyc`会加速后续加载）
2. 知识库实例已在`_course_cache`缓存，切换课程无需重读磁盘
3. 首次AI调用较慢为正常现象（模型推理耗时）

---

*文档版本：v1.0*  
*最后更新：2026年6月*