# 个性化学习资源智能体系统 —— 测试说明书

> **文档版本**：v2.1（依据 A3.md "八、文档及其他要求" 重写）
> **更新日期**：2026 年 6 月
> **被测系统**：多智能体个性化学习资源生成平台
> **测试范围**：AI 客户端层 / 多智能体调度层 / 课程知识库层 / 持久化数据库层 / 资源生成渲染层 / Streamlit 前端交互层 / 端到端全链路
> **设计理念**：以"需求层面"（大学生学习需求）与"技术开发层面"（AI 融合与创新实践）的双视角驱动测试设计

---

## 第一部分：测试概述与体系设计

### 1.1 测试的双层面设计

依据 A3.md 第八部分"八、文档及其他要求"，测试体系沿两个核心维度展开：

| 维度 | 对 A3 要求的映射 | 本说明书测试策略 |
|------|-----------------|----------------|
| **需求层面** | 深入理解和研究新时代大学生的学习需求，结合前沿 AI 技术的应用场景完成系统性的需求分析 | 围绕"画像构建 → 资源生成 → 路径规划 → 智能辅导 → 学习评估"五大业务闭环，设计覆盖真实高校课程的数据驱动用例；验证 AI 生成的资源是否贴合学生的认知水平与学习目标 |
| **技术开发层面** | 详细阐述智能体的设计、开发、测试、部署全流程，重点说明前沿 AI 技术在系统中的融合应用思路、创新实践和用户体验提升策略 | 测试 `MultiAgentScheduler` 的任务调度与子智能体协作，`BaseAIClient` / `OpenAIClient` 的抽象与容错，`ResourceGenerator` 的 PNG 渲染，`KnowledgeBase` 的多课程切换，SQLite 的 CRUD 与 `[AI错误]` 标记体系；同时评估前端交互的流畅度与界面美观 |

### 1.2 被测模块与源码映射

```
                         ┌───────────────────────────┐
                         │   Streamlit 前端 UI       │
                         │  (src/frontend/app.py)    │
                         └──────────┬────────────────┘
                                    │（会话状态 / 表单 / Markdown 渲染）
                         ┌──────────▼────────────────┐
                         │   MultiAgentScheduler     │  ← 第 3 节 功能测试
                         │  (agents/multi_...)       │
                         │   ┌─ ProfileAgent        │
                         │   ├─ ResourceAgent       │  ─┐
                         │   │  ├─ DocumentAgent    │   │
                         │   │  ├─ MindmapAgent     │   │
                         │   │  ├─ QuestionBankAgent│   │→ 5 类子智能体
                         │   │  ├─ CodeAgent        │   │
                         │   │  └─ ReadingMaterialAgent │
                         │   ├─ PathAgent           │
                         │   ├─ TutoringAgent       │
                         │   └─ EvaluationAgent     │
                         └──┬────────────┬──────────┘
                            │（文本/JSON）│
              ┌─────────────▼──┐    ┌────▼────────────┐
              │ BaseAIClient   │    │ KnowledgeBase    │  ← 第 2.1 / 2.2 节
              │ OpenAIClient   │    │ (course_kb JSON) │  单元测试
              │ MockClient     │    └────────┬─────────┘
              └──────┬─────────┘             │
                     │ HTTP POST              │ 本地 JSON
              ┌──────▼─────────┐     ┌──────▼───────────┐
              │ 讯飞/OpenAI    │     │ ResourceGenerator │ ← 思维导图渲染
              │ 模型 API       │     │  (graphviz PNG)   │   第 2.3 节
              └────────────────┘     └─────────┬─────────┘
                                                 │
              ┌─────────────────────────────────▼──────────────────────────┐
              │ Database (SQLite) — student_profiles / learning_paths /   │
              │          resources  (src/core/database.py)                │
              └───────────────────────────────────────────────────────────┘
```

### 1.3 测试类型总览

| 测试类型 | 所在章节 | 关注点 | 工具 / 方式 |
|---------|---------|--------|------------|
| 单元测试（Unit） | 第 2 节 | 各模块方法级正确性与边界条件 | Python REPL / 脚本断言 |
| 智能体功能测试 | 第 3 节 | 5 大业务流程的端到端输出质量 | Streamlit UI 交互 + 人工审阅 |
| 集成与全链路测试 | 第 4 节 | 多模块数据流贯通、多课程切换 | 端到端操作 + 数据库记录校验 |
| 异常与健壮性测试 | 第 5 节 | 网络失败、JSON 不规范、依赖缺失 | 模拟断网 / 修改源文件 / 断依赖 |
| 非功能性测试 | 第 6 节 | 响应时间、UI 美观、防幻觉、安全 | 计时工具 + 人工评审 + 敏感输入 |
| 知识库数据质量测试 | 第 7 节 | 两门课程 JSON 完整性、代码可运行 | 脚本校验 + 复制执行 |
| 可部署性测试 | 第 8 节 | 一键启动、环境自检、首屏渲染 | `streamlit run` + 浏览器 |
| 验收测试清单 | 第 9 节 | 逐项对齐 A3 赛题第 3/4 项 | 清单打钩 |

### 1.4 测试环境与数据

| 项目 | 规格 |
|------|------|
| 操作系统 | Ubuntu 22.04 LTS / Windows 11 / macOS 14（三平台交叉验证） |
| Python 版本 | 3.12 |
| 浏览器 | Chrome 120+ / Firefox 120+ / Edge 120+ |
| 数据库 | SQLite 3.x（随 Python 发行） |
| 系统依赖 | `graphviz`（`dot` 命令）—— 思维导图 PNG 渲染必需 |
| Python 依赖 | `streamlit`、`requests`、`graphviz`、`python-dotenv` |
| AI 服务 | 兼容 OpenAI Chat Completions 协议（讯飞星火等） |
| 知识库 | `data/course_kb/data_structures/`（数据结构，9 章）与 `data/course_kb/advanced_mathematics/`（高等数学，8 章） |
| 会话与持久化 | `data/user_profiles/profiles.db`（学生画像、学习路径、资源记录） |

### 1.5 核心创新点的测试重点

本系统在"技术开发层面"的创新点对应以下测试重点：

1. **多智能体架构**：5 个子智能体（文档 / 思维导图 / 题库 / 代码 / 阅读材料）协作生成内容 —— 验证 `ResourceAgent.run()` 返回结构的完整性与类型一致性。
2. **`[AI错误]` 标记体系**：对网络 / 鉴权 / 解析 / 空响应等统一采用可 greppable 的错误标记 —— 验证前端对红色错误 banner 的正确展示。
3. **知识库优先原则**：LLM 生成时先从课程 JSON 注入上下文，降低幻觉 —— 验证 `build_prompt_context()` 返回内容被合理引用。
4. **`MockClient` 离线体验**：无需真实 API 即可完成全部界面流程 —— 验证离线演示可运行性。
5. **Graphviz PNG 渲染**：将 LLM 返回的 JSON 树可视化为思维导图 —— 验证渲染文件非空且可正常打开。

---

## 第二部分：单元测试

### 2.1 KnowledgeBase 模块 —— 课程知识库

**源码路径**：`src/core/knowledge_base.py`

#### TC-UB-001：课程列表扫描

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证 `list_courses()` 能正确发现多课程目录结构并解析 manifest |
| 前置条件 | `data/course_kb/` 下至少包含 `data_structures/` 与 `advanced_mathematics/` 目录，各含 `course_manifest.json` |
| 执行脚本 | `python -c "from src.core.knowledge_base import list_courses; cs=list_courses(); [print(c) for c in cs]"` |
| 预期结果 | ① 返回列表长度 ≥ 2；② 每一项包含 `id`、`course_name`、`course_name_en` 三个字段；③ `id` 值为 `"data_structures"` 与 `"advanced_mathematics"` |
| 断言代码 | `assert any(c['id']=='data_structures' for c in cs) and any(c['id']=='advanced_mathematics' for c in cs)` |

#### TC-UB-002：按课程 ID 获取知识库实例

| 项目 | 内容 |
|------|------|
| 测试步骤 | 分别调用 `get_knowledge_base("data_structures")` 和 `get_knowledge_base("advanced_mathematics")`，检查返回对象的 `course_name` 与 `list_chapters()` |
| 预期结果 | ① 实例创建无异常；② `data_structures.course_name` 为"数据结构"；③ `advanced_mathematics.course_name` 为"高等数学"；④ 章节数分别 ≥ 9 与 ≥ 8 |

#### TC-UB-003：章节字段完整性校验

| 项目 | 内容 |
|------|------|
| 测试目的 | 确认 `get_chapter()` 对所有章节 JSON 返回的结构字段齐全，避免 KeyError |
| 测试步骤 | 遍历 `list_chapters()` 中每一章，对每个章节 JSON 断言下列字段存在：`summary`（字符串）、`key_points`（非空列表，每项含 `name`、`content`）、`code_examples`（列表，每项含 `title`、`language`、`code`）、`questions`（列表，每项含 `q`、`a`、`explanation`）、`reading_materials`（列表，每项含 `title`、`type`、`summary`、`difficulty`、`order`、`link`） |
| 预期结果 | 所有章节通过字段检查；不存在字段缺失或类型错误 |

#### TC-UB-004：知识点关键词搜索

| 项目 | 内容 |
|------|------|
| 测试数据 | `"链表"`（数据结构）/ `"导数"`（高等数学）/ `"时间复杂度"`（数据结构） |
| 测试步骤 | 对不同课程分别调用 `kb.search_knowledge_point(keyword)` |
| 预期结果 | ① 返回列表非空（至少有 1 条命中）；② 每条命中包含 `chapter_id`、`chapter_title`、`content` 字段；③ `content` 非空字符串 |

#### TC-UB-005：构建 LLM 提示上下文

| 项目 | 内容 |
|------|------|
| 测试步骤 | ① `kb.build_prompt_context("排序算法")`（有匹配主题）；② `kb.build_prompt_context("无意义字符串XYZABC")`（无匹配） |
| 预期结果 | ① 第一种情况：文本中包含课程名 + 若干"知识点：xxx（章节，难度：yyy）"条目；② 第二种情况：退化为展示课程章节列表而非空串；③ 文本长度 ≤ `max_chars`，截断处有 `...（已截断）` 标识 |

#### TC-UB-006：不存在课程 ID 的容错

| 项目 | 内容 |
|------|------|
| 测试步骤 | 调用 `get_knowledge_base("non_existent_course_999")` |
| 预期结果 | 不抛异常；系统默认退化为使用第一门可用课程或空课程实例；前端页面仍可正常渲染，无 Python traceback |

#### TC-UB-007：`_course_cache` 生效性

| 项目 | 内容 |
|------|------|
| 测试步骤 | 连续调用两次 `get_knowledge_base("data_structures")`，用 `time.perf_counter()` 测量耗时；并断言两次返回的是同一对象（`id(a) == id(b)`） |
| 预期结果 | 第二次调用耗时显著小于第一次（典型：首次 ~100ms，二次 < 1ms）；且对象引用一致 |

---

### 2.2 Database 模块 —— SQLite 持久化

**源码路径**：`src/core/database.py`

#### TC-UB-101：表结构初始化

| 项目 | 内容 |
|------|------|
| 测试目的 | 验证三张核心表能被正确创建 |
| 测试脚本 | ```python import tempfile, sqlite3, json; from src.core.database import Database; p=tempfile.mktemp(suffix='.db'); db=Database(db_path=p); db.create_tables(); conn=sqlite3.connect(p); names=set(r[0] for r in conn.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()); print(names); assert names=={'student_profiles','learning_paths','resources'}``` |
| 预期结果 | 返回表集合精确为 `{'student_profiles', 'learning_paths', 'resources'}` |

#### TC-UB-102：学生画像 CRUD

| 项目 | 内容 |
|------|------|
| 测试步骤 | ① `add_student_profile("u001", [0.7, 0.6, 0.8, 0.5, 0.6, 0.7], {"major":"计算机", "weak_points":["链表"]})`；② `get_student_profile("u001")` 并断言 `feat1..feat6` 数值一致且 `extra` 字段能反序列化为 dict；③ `update_student_profile("u001", features=[...], extra={"major":"计算机","note":"已更新"})`；④ `delete_student_profile("u001")` 后再次 `get_student_profile("u001")` 返回 `None` |
| 预期结果 | 全流程无异常；`update` / `delete` 返回值 ≥ 1（影响行数） |

#### TC-UB-103：特征维度非法值拦截

| 项目 | 内容 |
|------|------|
| 测试输入 | 传入 5 个特征（`[1,2,3,4,5]`）、7 个特征、或包含字符串的列表 `[1,2,3,"a",5,6]` |
| 预期结果 | 抛出 `ValueError` 或 `TypeError`，不脏写数据库 |

#### TC-UB-104：学习路径存储与排序

| 项目 | 内容 |
|------|------|
| 测试步骤 | 插入三条记录 `(uid, 2, 0.0, "Step 2")`、`(uid, 1, 0.0, "Step 1")`、`(uid, 3, 0.0, "Step 3")`（打乱顺序），再用 `get_learning_path(uid)` 取回 |
| 预期结果 | 返回列表严格按 `step` 升序排列为 `[Step 1, Step 2, Step 3]` |

#### TC-UB-105：资源记录的元数据反序列化

| 项目 | 内容 |
|------|------|
| 测试步骤 | `add_resource(uid, "res_01", "mindmap", "<content>", {"topic":"二叉树","chapter":"ch05","difficulty":"中"})` → `get_resource("res_01")` 检查字段 |
| 预期结果 | `metadata` 字段能反序列化为 dict，包含 `topic`、`chapter`、`difficulty`；`resource_type == "mindmap"` |

#### TC-UB-106：唯一约束与覆盖写入

| 项目 | 内容 |
|------|------|
| 测试步骤 | 对同一 `user_id` / `resource_id` 连续两次调用 `add_resource`（不同 content） |
| 预期结果 | 第二次调用覆盖旧值，不抛重复键错误（使用 `INSERT OR REPLACE`）；最后一次 `get_resource` 返回新 content |

#### TC-UB-107：数据库路径自动创建

| 项目 | 内容 |
|------|------|
| 测试步骤 | 指定一个不存在的深层路径（如 `/tmp/a/b/c/d/profiles.db`）创建 `Database` 实例并执行 `create_tables()` |
| 预期结果 | 上级目录被 `mkdir -p` 式自动创建；表创建成功不抛异常 |

---

### 2.3 ResourceGenerator 模块 —— 思维导图渲染

**源码路径**：`src/core/resource_generator.py`

#### TC-UB-201：JSON 树渲染 PNG

| 项目 | 内容 |
|------|------|
| 输入结构 | `{"title":"测试知识树","children":[{"title":"基础概念","note":"包含定义与术语"},{"title":"应用场景","children":[{"title":"场景A"},{"title":"场景B"}]}]}` |
| 测试脚本 | `ResourceGenerator(work_dir="/tmp/test_gen").render_mindmap(structure, output_name="ut_mindmap", size="8,5", dpi=150)` |
| 预期结果 | ① 不抛 `ExecutableNotFound`、`CalledProcessError`；② 生成 PNG 文件，`os.path.getsize(out) > 5_000`（至少几 KB，避免 0 字节空文件）；③ 文件路径与 `output_name` 匹配 |

#### TC-UB-202：超长节点文本稳健渲染

| 项目 | 内容 |
|------|------|
| 输入 | 构造 `title` 长度 > 240 字符、`note` 也超长的节点 |
| 预期结果 | 渲染不抛异常（内部有 `label[:237]+"..."` 截断）；生成的 PNG 文件非空 |

#### TC-UB-203：缺失 children 的退化结构

| 项目 | 内容 |
|------|------|
| 输入 | `{"title":"单节点"}`（无 children 字段） |
| 预期结果 | 不抛异常；生成仅含一个根节点的合法 PNG（至少 1 节点渲染成功） |

#### TC-UB-204：缺 graphviz 系统依赖时的失败提示

| 项目 | 内容 |
|------|------|
| 测试步骤 | 临时将系统 `dot` 命令重命名或断 PATH 后调用 `render_mindmap()` |
| 预期结果 | 捕获 `graphviz.backend.ExecutableNotFound`；调用方能展示"请安装 graphviz"提示而不崩 |

---

### 2.4 BaseAIClient / OpenAIClient 模块 —— AI 客户端抽象

**源码路径**：`src/api/base.py`、`src/api/openai_client.py`、`src/api/ai_client.py`

#### TC-UB-301：配置项读取与脱敏打印

| 项目 | 内容 |
|------|------|
| 测试步骤 | `from src.api.ai_client import get_ai_client; client = get_ai_client()` |
| 预期结果 | ① stderr 输出清晰打印 `OPENAI_API_URL`、`OPENAI_MODEL`；② `OPENAI_API_KEY` 中间字符被替换（如 `sk-****-...`）避免泄露；③ `client` 实例具备 `generate_text`、`generate_mindmap`、`generate_questions`、`generate_reading_material`、`generate_code` 五个方法 |

#### TC-UB-302：API Key 缺失场景

| 项目 | 内容 |
|------|------|
| 测试步骤 | 临时清空 `os.environ['OPENAI_API_KEY']` 并让 `cfg` 也不含 key，实例化 client 后调用 `client.generate_text("问题")` |
| 预期结果 | 返回字符串以 `[AI错误]:missing_config` 起始；不抛异常到上层 |

#### TC-UB-303：无效 URL / 网络错误

| 项目 | 内容 |
|------|------|
| 测试步骤 | 将 `api_url` 改为 `http://127.0.0.1:1/does-not-exist` 后执行 `client.generate_text("问题")` |
| 预期结果 | 返回 `[AI错误]:connection_error ...`，含原异常信息 |

#### TC-UB-304：401 / 403 鉴权失败

| 项目 | 内容 |
|------|------|
| 测试步骤 | 传入明显无效的 key（如 `"INVALID"`）后执行任何 `generate_*` |
| 预期结果 | 返回 `[AI错误]:auth_error ...`，提示检查 OPENAI_API_KEY / 模型权限 |

#### TC-UB-305：JSON 宽容解析路径

| 项目 | 内容 |
|------|------|
| 测试输入 | 形如 `"废话文本{\"title\":\"二叉树\",\"children\":[{\"title\":\"遍历\"}]}"`（前后有垃圾）以及 `{'title':'单引号JSON'}` 的非标准 JSON |
| 测试方法 | 对 `generate_mindmap` / `generate_questions` 注入这些文本作为返回，或直接调用内部 `_extract_json_like()` 做单测 |
| 预期结果 | 能从中解析出合法结构（列表 / dict），不抛 `JSONDecodeError` 导致 UI 崩溃 |

#### TC-UB-306：非 JSON 返回的友好降级

| 项目 | 内容 |
|------|------|
| 测试步骤 | 让上游返回一段"仅文本、无任何 JSON 子串"的自然语言 |
| 预期结果 | `generate_mindmap` 返回 `{"_ai_error": True, "error_kind":"parse_error", ...}` 结构；前端能将其转为红色错误 banner 展示而非直接崩 |

#### TC-UB-307：`generate_questions` 与 `generate_reading_material` 的字段校验

| 项目 | 内容 |
|------|------|
| 测试输入 | 对主题"快速排序"调用 `generate_questions`；对"微积分基础"调用 `generate_reading_material` |
| 预期结果 | ① 题目列表长度 ≥ 3，每题至少含 `q`、`a` 两个字段；② 阅读材料列表长度 ≥ 3，每项含 `title`、`type`、`difficulty`、`order`、`summary`、`link` |

---

### 2.5 多智能体调度器 —— MultiAgentScheduler

**源码路径**：`src/agents/multi_agent_scheduler.py`

#### TC-UB-401：`execute_task` 任务分发

| 项目 | 内容 |
|------|------|
| 测试步骤 | ① `scheduler.execute_task("profile", "计算机大二学生")`；② `scheduler.execute_task("resource", {"course":"数据结构","kp":"快速排序","rtype":"mindmap"})`；③ `scheduler.execute_task("unknown_task", {})`；④ `scheduler.execute_task("resource", None)`（空 payload） |
| 预期结果 | ①、② 返回结构化 dict 或错误 dict（非抛异常）；③ 返回 `{"error": "Unknown task type: unknown_task"}`；④ 返回 `{"error": "No input provided for resource task"}`；全部分支均不抛未捕获异常 |

#### TC-UB-402：`run_full_flow` 全流程流水线

| 项目 | 内容 |
|------|------|
| 测试步骤 | `scheduler.run_full_flow("大二学生，数据结构薄弱", "数据结构")` |
| 预期结果 | 返回 dict 必须同时包含 5 个 key：`profile`、`resources`（自身含 `document`/`mindmap`/`question_bank`/`code`/`reading_material`）、`plan`、`tutoring`、`evaluation`（其中 `tutoring` 与 `evaluation` 允许为 `None` 但必须在结果中存在 key） |

---

## 第三部分：智能体功能测试（业务层）

### 3.1 ProfileAgent —— 对话式学习画像构建

**业务目标**（A3 第 3 项）：摒弃传统表单，通过自然语言自动抽取特征，构建不少于 6 个维度的动态学生画像。

#### TC-FN-001：完整画像生成（6+ 维度）

| 项目 | 内容 |
|------|------|
| 前端入口 | 画像构建页面 |
| 测试输入（贴合大学生真实需求） | `"我是计算机专业大二学生，已经学过 C 语言与离散数学，最近感觉数据结构中的链表、二叉树部分理解不深，希望加强算法设计与代码实现能力；平时更喜欢通过视频和动手编程来学习"` |
| 测试步骤 | ① 在文本框输入上述描述 → ② 点击"生成画像" → ③ 等待 spinner 结束 → ④ 检查结果卡片各字段 → ⑤ 检查数据库 |
| 预期结果（需求层面验证） | ① 生成结构化画像（dict）；② ≥ 6 个维度字段（专业 / 知识水平 / 认知风格 / 薄弱环节 / 学习目标 / 学习建议），中文输出；③ `weak_points` 字段包含"链表"、"二叉树"等与输入高度相关关键词；④ `cognitive_style` 反映"视频 / 动手编程"风格；⑤ `student_profiles` 表新增一条 feat 向量记录 |
| 技术层面验证 | ① 前端无错误 banner；② spinner 结束前显示"正在分析..."；③ 不出现 `[AI错误]` 前缀 |

#### TC-FN-002：短输入 / 模糊输入稳健性

| 项目 | 内容 |
|------|------|
| 测试输入 | `"想学机器学习"` / `"我基础不好"` / `"数学好难"` |
| 预期结果 | 仍能生成合理画像（至少 3 个字段被填充，其他使用通用/默认描述），不返回 `{}` 且不抛异常 |

#### TC-FN-003：空输入 / 空白输入拦截

| 项目 | 内容 |
|------|------|
| 测试步骤 | 什么都不输，或只输入若干空格 / 换行，直接点击"生成画像" |
| 预期结果 | 前端黄色警告 `请先输入学生信息`，**不触发任何 API 调用**（减少 token 消耗） |

#### TC-FN-004：画像随学随新 —— 动态更新

| 项目 | 内容 |
|------|------|
| 测试步骤 | 先以输入 A 生成画像并持久化到 `student_profiles`；再用同一 `user_id` 输入完全不同的描述 B（例如把"计算机"换成"应用数学"），再次点击生成 |
| 预期结果 | 数据库中该 `user_id` 的 `feat1..feat6` 数值与 `extra` 字段被 `INSERT OR REPLACE` 覆盖更新；前端展示新的画像卡片 |

---

### 3.2 ResourceAgent + 5 子智能体 —— 多模态资源生成

**A3 要求**：由不同角色智能体协作完成至少 5 种类型的个性化资源生成。

#### TC-FN-101：教学文档生成（DocumentAgent）

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程 = 数据结构；知识点 = `"快速排序"`；类型 = `document` |
| 前置条件 | 已选择课程 → `kb.build_prompt_context("快速排序")` 能返回上下文 |
| 预期结果（需求层面） | ① 返回 Markdown 文本 ≥ 300 字，包含"算法原理 / 步骤 / 复杂度 / 伪代码或示例 / 适用场景"等章节；② 文档中出现知识库章节名（如"第8章 排序"）或与 `key_points` 对齐的术语；③ 无 `[AI错误]` 前缀；④ 语言为中文、排版易读 |

#### TC-FN-102：思维导图（MindmapAgent）→ PNG 渲染

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程 = 高等数学；知识点 = `"微分中值定理"`；类型 = `mindmap` |
| 测试步骤 | ① 点击"生成" → ② 页面显示 JSON 树预览 + 渲染后的 PNG 预览 → ③ 点击"下载 PNG"保存 → ④ 用系统图片查看器打开 |
| 预期结果 | ① JSON 树根节点包含 `title`、一级 children ≥ 3；② PNG 横向布局（`rankdir=LR`），节点文字清晰可读、无乱码；③ 文件大小在 20KB ~ 2MB 区间；④ 文件名含知识点关键词（如 `weifen_zhongzhi_dingli.mindmap.png`） |

#### TC-FN-103：题库结构化渲染（QuestionBankAgent）

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程 = 数据结构；知识点 = `"二叉树遍历"`；类型 = `question_bank` |
| 预期结果 | ① 返回 ≥ 5 道题目；每题含 `q` / `a` / `explanation` 三个字段；② 默认折叠答案，点击"查看答案与解析"展开；③ `q` 与 `a`/`explanation` 语义匹配（不出现"答案略"之类占位） |

#### TC-FN-104：代码示例生成（CodeAgent）

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程 = 数据结构；知识点 = `"哈希表冲突解决（链地址法）"`；类型 = `code` |
| 预期结果 | ① 返回完整可运行 Python 代码（含 `class` 或完整 `main`）；② 顶部有中文注释说明；③ Streamlit `st.code()` 语法高亮正常；④ **人工可验证**：复制代码到本地 Python 解释器执行，执行基本操作（插入 / 查询 / 冲突链打印）无语法错误且结果正确 |

#### TC-FN-105：拓展阅读推荐（ReadingMaterialAgent）

| 项目 | 内容 |
|------|------|
| 测试参数 | 课程 = 高等数学；知识点 = `"定积分的应用"`；类型 = `reading_material` |
| 预期结果 | ① 返回列表长度 ≥ 3；② 每条包含 `title` / `type` / `summary` / `difficulty` / `order` / `link` 六个字段；③ 按 `order` 升序展示；④ 页面提供"导出 JSON"按钮，下载文件能被 `json.load` 解析 |

#### TC-FN-106：资源生成前置参数校验

| 项目 | 内容 |
|------|------|
| 测试步骤 | ① 课程下拉为空、点击"生成资源"；② 知识点为空、点击"生成资源" |
| 预期结果 | 前端黄色警告提示，**不触发 API 调用**（network panel 无请求） |

---

### 3.3 PathAgent —— 个性化学习路径规划

**A3 要求**：结合大模型对学生专业、学习进度与偏好的分析，为学生规划科学、动态的个性化学习路径。

#### TC-FN-201：基于画像的路径生成

| 项目 | 内容 |
|------|------|
| 前置条件 | 画像构建页已生成画像并写入 `st.session_state.profile` 与 `student_profiles` 表 |
| 测试步骤 | 进入"学习路径"页 → 点击"生成个性化学习路径" |
| 预期结果 | ① 生成阶段性文本（基础巩固 → 核心学习 → 综合提升），分段标题清晰；② 路径中出现知识库章节名（如"第2章 线性表"、"第5章 树与二叉树"）；③ `learning_paths` 表新增 ≥ 1 条带 step 的记录 |

#### TC-FN-202：无画像时的提示

| 项目 | 内容 |
|------|------|
| 测试步骤 | 清除 session 或刷新浏览器后直接进入路径页点击"生成" |
| 预期结果 | 显示橙色提示 `请先在画像构建页面生成画像`，**不触发 API** |

---

### 3.4 TutoringAgent —— 智能辅导

**A3 要求（加分项）**：提供即时、多模态的答疑解惑服务，智能体结合大模型知识支持实现针对性学习引导。

#### TC-FN-301：单轮答疑

| 项目 | 内容 |
|------|------|
| 测试问题 | `"什么是时间复杂度 O(n log n)？请举例说明，最好给一段 Python 代码"` |
| 预期结果 | ① 用户消息在右侧气泡展示；② AI 返回 Markdown，包含"简要回答 / 分步说明 / 代码示例 / 参考拓展"四部分结构；③ 代码块语法高亮正常；④ 响应时间 ≤ 15 秒 |

#### TC-FN-302：多轮上下文对话

| 项目 | 内容 |
|------|------|
| 测试步骤 | 第一轮问 `"什么是 AVL 树？"`；第二轮问 `"它和红黑树有什么区别？"`；第三轮问 `"各给我一个 Python 实现"` |
| 预期结果 | 第二轮回答与"AVL 树"强关联（不会被重新定义为"树"）；第三轮给出两段可独立运行的 Python 代码；历史消息按时间顺序完整展示 |

#### TC-FN-303：对话历史跨页保持

| 项目 | 内容 |
|------|------|
| 测试步骤 | 在辅导页聊 5 轮 → 切换到"知识库"页浏览 10 秒 → 再切回"智能辅导" |
| 预期结果 | 5 条历史消息完整显示，顺序与原始输入一致（左侧 AI / 右侧用户） |

---

### 3.5 EvaluationAgent —— 学习效果评估

**A3 要求（加分项）**：实时跟踪学习行为与练习测试情况，依托大模型数据分析完成多维度评估，并根据结果动态调整推荐策略。

#### TC-FN-401：对生成文档的质量评分

| 项目 | 内容 |
|------|------|
| 测试输入 | 将 TC-FN-101 生成的"快速排序教学文档"传入 `EvaluationAgent.run()` |
| 预期结果 | ① 返回结构化评分（如 1-5 分，或描述性评价 + 数值指标）；② 至少包含"改进建议"字段；③ 不抛异常、无 `[AI错误]` 前缀 |

#### TC-FN-402：模拟"学生错题反馈"评估

| 项目 | 内容 |
|------|------|
| 测试输入 | 构造学生错误答案：`"我认为快速排序最坏时间复杂度是 O(n log n)"`，调用评估接口 |
| 预期结果 | 返回对错误点的诊断（"应为 O(n²)"）+ 对应知识点章节的推荐阅读链接或章节名（如"参见 第8章 排序"） |

---

## 第四部分：集成与端到端测试

### 4.1 完整学习场景 —— 端到端闭环

#### TC-INT-001：大学生从画像到资源到路径到辅导到评估的完整闭环

```
场景设定：计算机专业大二学生，已学 C 语言 / 离散数学，对数据结构中链表与二叉树掌握欠佳，
         目标是在本学期期末前补齐算法与编程实践能力。
```

**执行步骤**：

1. **画像构建**
   - 输入：`"我是计算机专业大二学生，已经学过 C 语言与离散数学，最近感觉数据结构中的链表、二叉树部分理解不深，希望加强算法设计与代码实现能力；平时更喜欢通过视频和动手编程来学习"`
   - 验证：① 生成 ≥ 6 维度画像；② `student_profiles` 表新增 1 行；③ `weak_points` 字段包含"链表"、"二叉树"

2. **知识库浏览**
   - 操作：顶部下拉切换到"数据结构"课程 → 浏览 ch02 线性表、ch05 树与二叉树
   - 验证：① `kb.course_name` 为"数据结构"；② ch02 与 ch05 的 `summary`、`key_points`、`code_examples`、`questions`、`reading_materials` 均正常渲染

3. **多模态资源生成**（覆盖 A3 要求的 5 类资源）
   - Document：`链表` → 预期 ≥ 300 字 Markdown，含"插入/删除/查找/复杂度"章节
   - Mindmap：`二叉树` → 预期 PNG 文件 ≥ 10KB，根节点为"二叉树"，children 含"遍历"、"平衡"等
   - Question Bank：`二叉树遍历（先序/中序/后序/层序）` → 预期 5 题，每题 `q/a/explanation` 完整
   - Code：`哈希表（链地址法）` → 预期 Python 代码可独立运行，`insert / search / delete` 方法正确
   - Reading Material：`算法进阶（AVL / 红黑树 / B 树）` → 预期 ≥ 3 条推荐，按 `order` 升序

4. **个性化学习路径**
   - 操作：基于画像点击"生成学习路径"
   - 验证：① 路径文本包含"重点：链表、二叉树"；② 路径中出现"第2章 线性表"、"第5章 树与二叉树"等章节名；③ `learning_paths` 表新增 ≥ 1 条记录

5. **智能辅导**
   - 输入轮 1：`"AVL 旋转是如何实现的？"`
   - 输入轮 2：`"给我 Python 代码"`
   - 输入轮 3：`"它和红黑树相比有什么优缺点？"`
   - 验证：上下文连贯；代码可复制独立运行；响应时间单轮 ≤ 15 秒

6. **学习评估**
   - 操作：将步骤 3 的 DocumentAgent 输出传入 `EvaluationAgent` 做质量评估
   - 验证：返回结构化评分（如 4/5）+ "改进建议"字段；评估结果无 `[AI错误]` 标记

**验收标准（全部满足方算通过）**：
- [x] 全流程无 `UncaughtException`、无白屏、无红色 Python traceback
- [x] `student_profiles` 新增 1 条、`learning_paths` 新增 ≥ 1 条、`resources` 新增 ≥ 5 条
- [x] 思维导图 PNG 至少 1 个，文件 ≥ 10KB，能被系统图片查看器正常打开
- [x] `st.session_state` 跨页保持画像、对话历史等状态
- [x] 整个闭环的总用时（不含人工审阅）在良好网络条件下 ≤ 180 秒

---

### 4.2 多课程切换 —— 上下文传播正确性

#### TC-INT-101：课程切换后资源上下文正确

| 项目 | 内容 |
|------|------|
| 测试步骤 | ① 选"数据结构"课程 → 生成"链表"文档，观察文档中出现"链表"、"第2章 线性表"等关键词；② 顶部下拉立即切换到"高等数学"课程 → 重新生成"导数"文档 |
| 预期结果 | ① 切换后的文档主题从"数据结构"完全切换到"高等数学"；② 文档中出现"极限"、"导数"、"微分"等关键词而非"链表"；③ `kb.course_name` 在切换后确实更新为"高等数学"；④ 不发生"旧课程上下文泄漏" |

---

### 4.3 AI 客户端双路径 —— Mock 离线体验

#### TC-INT-201：MockClient 离线演示

| 项目 | 内容 |
|------|------|
| 测试步骤 | 手动将 `get_ai_client()` 返回值替换为 `MockClient()`（或临时清空所有 env key 让系统走 mock 分支），重复执行画像 / 资源 / 路径 / 辅导 / 评估流程 |
| 预期结果 | ① 所有功能可在无 API 环境下运行；② 返回结构字段齐全（保持与真实 API 调用同样的结构签名）；③ 前端展示与真实调用无明显差异（除了内容为 mock 数据） |

---

## 第五部分：异常与健壮性测试

### 5.1 API 相关异常

#### TC-ERR-001：网络超时 / 连接失败

| 项目 | 内容 |
|------|------|
| 模拟方法 | ① 断 Wi-Fi；② 将 `api_url` 改为不可达地址；③ 修改 `openai_client.py` 内部 timeout 为 0.01 |
| 测试步骤 | 触发画像 / 资源 / 辅导任一功能 |
| 预期结果 | ① 不抛未捕获异常；② 前端显示红色错误 banner，文案以 `[AI错误]:connection_error` 起始，包含原异常信息；③ 其他页面（知识库浏览、路径查看等）保持可用 |

#### TC-ERR-002：API Key 认证失败

| 项目 | 内容 |
|------|------|
| 模拟方法 | 将 `OPENAI_API_KEY` 改为 `"INVALID-KEY-12345"` 后调用画像功能 |
| 预期结果 | 返回 `[AI错误]:auth_error ...`，提示"请检查 OPENAI_API_KEY / 模型权限"；前端不崩，用户可重新修改配置并重试 |

#### TC-ERR-003：LLM 返回非 JSON —— 宽容解析路径

| 项目 | 内容 |
|------|------|
| 模拟输入 | 构造 `"以下是我为您生成的思维导图：\n\n{\n  \"title\": \"二叉树\",\n  \"children\": [...] \n}\n\n希望对您有帮助！"` 这样的"包裹式"文本；以及 `"{'title':'单引号风格','children':[]}"` 的非标准 JSON |
| 预期结果 | ① `generate_mindmap` 能从"包裹式"文本中提取 JSON 并正确解析；② 对单引号 JSON 也能降级解析（内部 `replace("'",'"')` 路径）；③ 页面不崩 |

#### TC-ERR-004：返回内容为空 / 仅空白字符

| 项目 | 内容 |
|------|------|
| 模拟方法 | 让上游 API 返回 `""` 或 `"   "`（纯空白） |
| 预期结果 | `OpenAIClient._format_response_text()` 返回 `[AI错误]:empty_response ...`，不抛 `NoneType` 异常；前端展示为红色错误 banner |

#### TC-ERR-005：HTTP 500 / 503 等服务端错误

| 项目 | 内容 |
|------|------|
| 模拟方法 | 将 `api_url` 临时指向一个返回 500 的测试服务，或用 mock `requests.post()` 抛出非 2xx 响应 |
| 预期结果 | 返回 `[AI错误]:http_error ...` 包含原状态码信息；不抛异常到上层 |

---

### 5.2 知识库数据异常

#### TC-ERR-101：章节 JSON 文件缺失

| 项目 | 内容 |
|------|------|
| 模拟方法 | 将 `data/course_kb/data_structures/ch02_linear_list.json`（或对应 manifest 中引用的章节 JSON）临时重命名后启动应用 |
| 预期结果 | ① `course_manifest.json` 仍列出该章节；② 点击展开该章时显示"暂无章节详情 / 章节数据加载失败"提示；③ 其他章节不受影响、可正常浏览 |

#### TC-ERR-102：章节 JSON 语法损坏

| 项目 | 内容 |
|------|------|
| 模拟方法 | 删除某章节 JSON 文件末尾 `}` 并重新启动 |
| 预期结果 | 应用不崩；该章显示加载错误提示；其他章节 JSON 正常解析与渲染 |

#### TC-ERR-103：知识点条目字段缺失

| 项目 | 内容 |
|------|------|
| 模拟方法 | 在某章节的 `key_points` 数组中插入 `{"name": "缺少 content 字段"}`（故意缺字段） |
| 预期结果 | `search_knowledge_point()` 对该条目返回 `content = ""`，不抛 `KeyError`；前端 UI 仍可渲染（空 content 时显示占位"暂无详细说明"） |

#### TC-ERR-104：manifest 章节引用与实际 JSON 不一致

| 项目 | 内容 |
|------|------|
| 模拟方法 | 在 `course_manifest.json` 中引用一个不存在的 `file` 名 |
| 预期结果 | `KnowledgeBase._load_all()` 跳过不存在文件但不抛异常；该章节显示"章节数据缺失"提示；课程其他章节不受影响 |

---

### 5.3 资源渲染与依赖异常

#### TC-ERR-201：系统级 graphviz 未安装

| 项目 | 内容 |
|------|------|
| 模拟方法 | 在未装 graphviz 的环境（或将系统 `dot` 重命名） |
| 预期结果 | ① 捕获 `ExecutableNotFound` 异常；② 在页面显示"思维导图渲染失败，请先安装 graphviz 系统依赖"提示；③ 同时展示原始 JSON 树结构让用户了解 LLM 输出了什么；④ 其他资源类型（文档 / 题库 / 代码 / 阅读材料）不受影响 |

#### TC-ERR-202：超大节点树导致渲染超时

| 项目 | 内容 |
|------|------|
| 测试输入 | 构造深度 5 + 每层 10 节点的巨型 JSON 树调用 `render_mindmap()` |
| 预期结果 | ① 或在合理时间内成功生成（图形库对大图形支持良好）；② 或因超时 / 资源不足失败并以用户可读形式提示；③ 前端不会被卡死超过 30 秒；④ Python 进程不会被 OOM kill（graphviz 本身对大图稳定，此条主要验证无内存泄漏） |

#### TC-ERR-203：JSON 树结构缺失 `title`

| 项目 | 内容 |
|------|------|
| 测试输入 | `{"children":[{"title":"节点1"}]}`（根节点无 title） |
| 预期结果 | `render_mindmap()` 抛出 `ValueError("structure 必须为包含 'title' 的 dict")`；上层调用方捕获后展示红色错误 banner |

---

### 5.4 数据库异常

#### TC-ERR-301：db 路径不可写

| 项目 | 内容 |
|------|------|
| 模拟方法 | 在 Linux 上 `mkdir -p /tmp/readonly && chmod 555 /tmp/readonly && touch /tmp/readonly/x.db && chmod 444 /tmp/readonly/x.db`，以此路径创建 `Database` 实例并执行 `add_student_profile()` |
| 预期结果 | 抛出 `sqlite3.OperationalError`（如 "attempt to write a readonly database"）；在 Streamlit 调用方被捕获后以红色错误提示展示 |

#### TC-ERR-302：并发写入 —— 同 user_id 多线程 / 多按钮重复点击

| 项目 | 内容 |
|------|------|
| 模拟方法 | 同一浏览器会话中，对"生成画像"按钮快速连点 5 次（模拟用户不耐烦行为） |
| 预期结果 | ① Streamlit spinner 只执行一次（前端按钮被 spinner 阻塞）；② `INSERT OR REPLACE` 幂等写入不产生脏数据；③ 最终 `student_profiles` 中该 user_id 只有 1 条记录 |

---

## 第六部分：非功能性测试

### 6.1 性能指标

#### TC-NF-001：核心功能响应时间（AI 调用）

| 项目 | 内容 |
|------|------|
| 测量方法 | 同账号、同网络、对同一提示词连续调用 5 次取平均值 |
| 验收阈值 | 画像构建 ≤ 15 秒；单类资源生成 ≤ 20 秒；辅导问答 ≤ 12 秒；学习评估 ≤ 10 秒 |
| 数据记录 | 记录每次调用的实际耗时，计算均值与标准差；记录异常调用（超时或失败）次数占比 |

#### TC-NF-002：知识库首次加载与缓存命中

| 项目 | 内容 |
|------|------|
| 测量脚本 | `python -c "import time; t=time.perf_counter(); from src.core.knowledge_base import get_knowledge_base; kb=get_knowledge_base('data_structures'); print(len(kb.list_chapters()),'章, 首次加载耗时', round(time.perf_counter()-t,4),'s'); t=time.perf_counter(); kb2=get_knowledge_base('data_structures'); print('二次加载', round(time.perf_counter()-t,4),'s')"` |
| 验收阈值 | 首次加载 ≤ 2 秒；第二次调用（命中 `_course_cache`） ≤ 0.2 秒 |

#### TC-NF-003：思维导图 PNG 文件体积

| 项目 | 内容 |
|------|------|
| 验收 | 常规知识点思维导图 ≤ 500KB；超大型结构（深度 5+，分支数多）最大 ≤ 2MB |

#### TC-NF-004：Streamlit 首屏启动时间

| 项目 | 内容 |
|------|------|
| 测量 | `time streamlit run src/frontend/app.py --server.headless=true --server.port=8765`（另起终端 `curl -s http://localhost:8765` 计时到 HTTP 200 返回） |
| 验收阈值 | 首次启动（cold start） ≤ 8 秒；二次启动 ≤ 4 秒 |

---

### 6.2 用户体验（UX）与界面美观

**A3 要求**：系统界面美观大方、简洁明了，交互逻辑清晰，符合现代 AI 产品交互规范（流式输出、Markdown 渲染、多模态内容卡片化展示）。

#### TC-NF-101：视觉与交互一致性

| 检查项 | 标准 | 结果 |
|--------|------|------|
| 导航按钮 | 侧栏 5+ 个页面按钮风格统一：深色背景 + hover 位移 + 圆角 + 左对齐 | |
| 页面标题 | 每个功能页使用相同字号（24-28px）、字体、间距与顶部 padding | |
| 卡片样式 | 内容卡片统一为圆角 + 柔和阴影 + 左右对称 padding，不出现贴边文字 | |
| 标签徽章 | 难度、资源类型标签风格一致，颜色与语义一致（绿=易 / 橙=中 / 红=难） | |
| 加载反馈 | AI 调用期间显示 spinner + 文字提示（"正在分析..."或"正在生成..."），无白屏 | |
| 字体大小 | 正文 14-16px，标题 20-28px，不出现超大或过小的不可读文字 | |
| 颜色对比 | 文字与背景色对比度满足 WCAG AA（≥ 4.5:1 正文，≥ 3:1 标题） | |
| 代码高亮 | `st.code()` 块使用深色主题 + 行号，语法高亮清晰可辨 | |
| 滚动条与溢出 | 长文档 / 大代码块出现内部滚动条而非破坏页面布局 | |

#### TC-NF-102：响应式布局

| 检查项 | 标准 | 结果 |
|--------|------|------|
| 桌面 1920×1080 | 内容区块居中，两侧留白合理，水平不溢出 | |
| 笔记本 1366×768 | 所有页面可完整浏览，无横向滚动条，按钮不被截断 | |
| 移动端 375×812（可选） | Streamlit 自动响应式，侧栏可折叠；文字在移动端可读 | |

#### TC-NF-103：错误与警告 UI 一致性

| 检查项 | 标准 |
|--------|------|
| 严重错误（API 失败 / 依赖缺失） | 红色 banner，以 `[AI错误]:xxx` 起始文案，下方附详细错误信息 |
| 警告（空输入 / 无画像） | 黄色警告，文字不包含 `[AI错误]` 标记 |
| 成功反馈 | 绿色成功提示或"✓ 已生成"卡片 |

---

### 6.3 内容安全与"防幻觉"

**A3 要求**：具备完善的"防幻觉"与内容安全过滤机制，确保生成的学术内容无事实性错误、无敏感违规信息。

#### TC-NF-201：学术事实正确性校验（人工抽查）

| 测试输入 | 预期（抽查要点） | 结果 |
|----------|------------------|------|
| 快速排序文档 | ① 平均复杂度 O(n log n)；② 最坏 O(n²)；③ 稳定? → 不稳定；④ partition 伪代码正确 | |
| 二叉树遍历题库 | ① 先序 = 根→左→右；② 中序 = 左→根→右；③ 后序 = 左→右→根；④ 层序 = BFS | |
| 时间复杂度讲解 | ① O(1) / O(log n) / O(n) / O(n log n) / O(n²) / O(2ⁿ) 定义正确；② 不出现"DFS 复杂度 O(E*V)"这类错误（应为 O(V+E)） | |
| 导数计算示例 | ① (sin x)' = cos x；② (eˣ)' = eˣ；③ (xⁿ)' = n·xⁿ⁻¹；不出现公式符号错误 | |
| 哈希表实现代码 | ① 链地址法/开放地址法区分清晰；② hash 函数有基本分布；③ 冲突处理正确，不会因 resize 丢数据 | |

#### TC-NF-202：敏感内容 / 违规内容拦截

| 测试输入 | 预期结果 |
|----------|---------|
| 含政治敏感的诱导性提示词 | AI 拒绝生成；前端展示"无法生成相关内容"或合规回复；系统不崩 |
| 暴力 / 违法辅导类提示词 | 同上，合规拒绝；不触发系统崩溃 |
| 涉及个人隐私类提示词 | AI 回复保持中立、不虚构个人信息 |

#### TC-NF-203：知识库优先原则 —— 降低幻觉

| 测试输入 | 预期 |
|----------|------|
| 针对课程已定义的知识点请求生成资源（"KMP 算法"、"红黑树"、"定积分"） | 生成结果术语、章节名称与 `course_kb/` 下定义保持一致；不出现与教材冲突的内容 |
| 对课程不存在的知识点请求生成 | 明确标注"本课程未覆盖该内容，以下为通用参考"或生成通用内容但与课程其他章节术语风格一致 |

---

### 6.4 可访问性与国际化基础

#### TC-NF-301：中文编码与显示

| 检查项 | 标准 | 结果 |
|--------|------|------|
| 页面 HTML 编码 | `<meta charset="utf-8">` | |
| 数据库存储 | 中文无乱码（Python sqlite3 默认 utf-8） | |
| API 调用 request headers | `Content-Type: application/json` + 支持 UTF-8 | |
| 文件名 / 路径 | 中文文件名在 Windows / Linux / macOS 三平台正常读写 | |

---

## 第七部分：课程知识库数据质量测试

### 7.1 数据结构课程

**路径**：`data/course_kb/data_structures/`

#### TC-DS-001：章节完整性

| 验证项 | 预期内容 | 实际 |
|--------|---------|------|
| 总章节数 | 9 章（ch01-ch09） | |
| ch01 绪论 | 数据结构基本概念 / 抽象数据类型 / 算法分析 / 时间复杂度 / 空间复杂度 | |
| ch02 线性表 | 顺序表 / 单链表 / 双向链表 / 循环链表 / 典型应用（多项式等） | |
| ch03 栈与队列 | 栈 / 括号匹配 / 表达式求值 / 队列 / 循环队列 / 双端队列 | |
| ch04 串、数组与广义表 | 字符串操作 / KMP 算法 / 数组存储 / 稀疏矩阵 / 广义表 | |
| ch05 树与二叉树 | 树的术语 / 二叉树性质 / 存储结构 / 遍历（先序/中序/后序/层序）/ 线索二叉树 / 哈夫曼树 | |
| ch06 图 | 存储结构 / DFS / BFS / 最小生成树 Prim / Kruskal / 最短路径 Dijkstra / Floyd / 拓扑排序 | |
| ch07 查找 | 顺序查找 / 折半查找 / 二叉排序树 / AVL 树 / 红黑树 / B 树 / 跳表 / 哈希表 | |
| ch08 排序 | 插入 / 冒泡 / 快速排序 / 选择 / 堆排序 / 归并 / 基数 / 算法对比 | |
| ch09 文件与外存结构 | 顺序文件 / 索引文件 / 散列文件 / 多级索引 | |
| 总学时 | 64 学时 | |

#### TC-DS-002：代码示例可运行性抽样

| 抽样章节 | 示例代码标题 | 执行结果（√ / × / 需修复） |
|---------|-------------|--------------------------|
| ch02 线性表 | 链表插入 / 删除 / 查找 | |
| ch05 树与二叉树 | 先序 / 中序 / 后序遍历（递归 + 非递归） | |
| ch08 排序 | 快速排序（含 partition） / 堆排序（含 heapify） | |
| ch06 图 | BFS / DFS 邻接表实现 | |

**人工执行步骤**：将每段代码复制到 Python 3.12 解释器执行，确认无语法错误、能用样例输入产生正确输出、包含必要注释。

#### TC-DS-003：题库题目抽样

| 章节 | 题目数 ≥ | 抽样检查 |
|------|---------|---------|
| ch05 树与二叉树 | 5 | 3 道题目包含清晰 `q` / `a` / `explanation` |
| ch07 查找 | 5 | 同上 |
| ch08 排序 | 5 | 同上 |

---

### 7.2 高等数学课程

**路径**：`data/course_kb/advanced_mathematics/`

#### TC-DS-101：章节完整性

| 验证项 | 预期内容 | 实际 |
|--------|---------|------|
| 总章节数 | 8 章（ch01-ch08） | |
| ch01 函数与极限 | 函数概念 / 极限定义 / 极限运算 / 无穷小 / 连续性 | |
| ch02 导数与微分 | 导数定义 / 求导法则 / 高阶导数 / 微分 / 隐函数求导 | |
| ch03 微分中值定理与导数应用 | 罗尔 / 拉格朗日 / 柯西 / 洛必达 / 泰勒公式 / 单调性 / 极值 / 凹凸性 | |
| ch04 不定积分 | 原函数 / 积分公式 / 换元法 / 分部积分 / 有理函数积分 | |
| ch05 定积分及其应用 | 定积分定义 / 牛顿-莱布尼茨 / 换元 / 分部 / 面积 / 体积 / 弧长 / 物理应用 | |
| ch06 微分方程 | 基本概念 / 一阶 / 可分离变量 / 齐次 / 线性 / 伯努利 / 二阶常系数 | |
| ch07 多元函数微分学 | 多元函数 / 偏导 / 全微分 / 复合函数求导 / 方向导数 / 梯度 / 极值 / 条件极值 | |
| ch08 多元函数积分学 | 二重 / 三重 / 累次积分 / 极坐标 / 应用 | |

#### TC-DS-102：数学术语与符号规范

| 抽样检查项 | 标准 | 结果 |
|-----------|------|------|
| 定理表述 | 罗尔定理、拉格朗日中值定理等表述与主流教材一致 | |
| 公式符号 | 使用 `∫` / `∑` / `lim` / `≈` / `≠` / `≤` / `≥` 等符号而非 ASCII 替代（避免用 `int` / `sum` 替代积分/求和符号） | |
| 例题解答步骤 | 步骤完整、推导过程清晰、不跳步 | |

---

### 7.3 manifest 结构一致性

#### TC-DS-201：两门课程 manifest 字段对齐

| 字段 | 数据结构课程必须包含 | 高等数学课程必须包含 |
|------|-------------------|-------------------|
| `course_name` | ✓ | ✓ |
| `course_name_en` | ✓ | ✓ |
| `chapters`（长度） | ≥ 9 | ≥ 8 |
| `total_hours` | 64 | ≠ 0 |
| 每章 `knowledge_points` | 非空数组 | 非空数组 |
| 每章 `file` 指向 | 对应 JSON 文件存在 | 对应 JSON 文件存在 |

---

## 第八部分：可部署性与冒烟测试

### 8.1 一键启动冒烟

#### TC-DEP-001：Streamlit 启动

| 项目 | 内容 |
|------|------|
| 步骤 | `pip install -r requirements.txt` → `streamlit run src/frontend/app.py` |
| 预期输出 | 控制台显示 `You can now view your Streamlit app in your browser. Local URL: http://localhost:8501`；浏览器访问后首页正常渲染，无 Python traceback |

#### TC-DEP-002：首次启动自动初始化

| 项目 | 内容 |
|------|------|
| 预期 | 首次启动自动完成：① 加载 `config/.env` 环境变量；② 初始化 `get_ai_client()` 实例（有 key 走 OpenAI/讯飞，无 key 走 mock）；③ 初始化 `MultiAgentScheduler`；④ 在 `data/user_profiles/` 下创建 `profiles.db`（若不存在） |

#### TC-DEP-003：环境依赖自检

| 项目 | 内容 |
|------|------|
| 测试脚本 | 启动时检测 Python 版本、`requests` / `graphviz` / `streamlit` 版本 |
| 预期 | 版本不满足时给出"建议升级到 ≥ x.y"提示（不强制退出应用运行） |

---

### 8.2 多平台兼容性矩阵

| OS | Python | streamlit | requests | 系统级 graphviz | 结果 |
|----|--------|-----------|----------|-----------------|------|
| Ubuntu 22.04 | 3.12 | 1.32+ | 2.31+ | `apt install graphviz` | |
| Windows 11 | 3.12 | 1.32+ | 2.31+ | 官网下载安装包 | |
| macOS 14 | 3.12 | 1.32+ | 2.31+ | `brew install graphviz` | |

---

## 第九部分：验收测试清单（与 A3 赛题对齐）

### 9.1 核心功能需求对齐（A3 第 3 项）

- [ ] **对话式学习画像自主构建**：≥ 6 个维度字段，支持画像动态更新与持久化到 `student_profiles` 表
- [ ] **多智能体协同的资源生成**：5 类资源（文档 / 思维导图 / 题库 / 代码示例 / 拓展阅读）均一键生成；输出引用知识库章节名
- [ ] **个性化学习路径规划**：基于画像生成阶段式路径文本，章节名与知识库一致；持久化到 `learning_paths` 表
- [ ] **智能辅导（加分项）**：多轮对话上下文保持；Markdown / 代码渲染正常；响应时间 ≤ 15 秒
- [ ] **学习效果评估（加分项）**：对生成资源给出结构化评分与改进建议；对学生错误答案能定位到章节

### 9.2 非功能需求对齐（A3 第 4 项）

- [ ] **界面美观大方**：渐变 / 卡片 / 按钮风格统一；无布局错位；hover 与 spinner 交互流畅
- [ ] **开源项目标注**：`requirements.txt` 列明第三方库；`app.py` 顶部注释列依赖与来源
- [ ] **防幻觉与内容安全**：人工抽查 ≥ 10 个生成内容无明显事实错误；敏感输入被合理拒绝
- [ ] **响应时间**：画像 ≤ 15s / 资源单类 ≤ 20s / 辅导 ≤ 12s
- [ ] **多模态内容呈现**：思维导图渲染为 PNG、代码高亮、题库可展开、文档 Markdown 排版

### 9.3 异常与健壮性（关键验收）

- [ ] 无 API Key 场景：所有 AI 功能返回 `[AI错误]:missing_config ...` 不崩
- [ ] 断网场景：前端红色错误 banner，其余 UI 可用
- [ ] 缺失章节 JSON：不影响其他章节浏览
- [ ] graphviz 未安装：显示降级提示 + JSON 结构
- [ ] 无效画像输入：黄色警告 + 不触发 API

### 9.4 知识库与数据完整性

- [ ] 数据结构课程 9 章齐全，每章 5 大类字段齐全
- [ ] 高等数学课程 8 章齐全，每章 5 大类字段齐全
- [ ] 代码示例抽样 ≥ 4 段可独立运行
- [ ] 题库题目抽样 ≥ 3 章节，每题含 `q / a / explanation`

### 9.5 性能验收（单点阈值）

- [ ] 知识库首次加载 ≤ 2s
- [ ] 二次加载 ≤ 0.2s（缓存生效）
- [ ] 思维导图 PNG ≤ 500KB（常规）/ ≤ 2MB（大型）
- [ ] Streamlit 冷启动 ≤ 8s

---

## 第十部分：测试执行记录与缺陷登记模板

### 10.1 测试执行记录表

| 用例编号 | 用例名称 | 测试日期 | 测试人员 | 测试环境 | 实际结果 | 耗时（秒） | 状态（通过/失败/阻塞） | 问题编号 |
|---------|---------|---------|---------|---------|---------|-----------|---------------------|---------|
| TC-UB-001 | 课程列表扫描 | | | | | | | |
| TC-UB-002 | 按课程 ID 获取知识库实例 | | | | | | | |
| TC-UB-003 | 章节字段完整性 | | | | | | | |
| TC-UB-004 | 知识点关键词搜索 | | | | | | | |
| TC-UB-101 | 表结构初始化 | | | | | | | |
| TC-UB-102 | 学生画像 CRUD | | | | | | | |
| TC-UB-201 | JSON 树渲染 PNG | | | | | | | |
| TC-UB-301 | AI 客户端配置读取 | | | | | | | |
| TC-UB-305 | JSON 宽容解析 | | | | | | | |
| TC-UB-401 | 调度器任务分发 | | | | | | | |
| TC-FN-001 | 6+ 维度画像生成 | | | | | | | |
| TC-FN-101 | 教学文档生成 | | | | | | | |
| TC-FN-102 | 思维导图 PNG 渲染 | | | | | | | |
| TC-FN-103 | 题库结构化渲染 | | | | | | | |
| TC-FN-104 | 代码示例生成（可运行） | | | | | | | |
| TC-FN-105 | 拓展阅读推荐 | | | | | | | |
| TC-FN-201 | 基于画像的学习路径 | | | | | | | |
| TC-FN-301 | 单轮辅导问答 | | | | | | | |
| TC-FN-302 | 多轮上下文对话 | | | | | | | |
| TC-FN-401 | 资源质量评分 | | | | | | | |
| TC-INT-001 | 完整学习闭环 | | | | | | | |
| TC-INT-101 | 课程切换上下文正确性 | | | | | | | |
| TC-ERR-001 | 网络超时 / 连接失败 | | | | | | | |
| TC-ERR-003 | JSON 宽容解析（包裹/单引号） | | | | | | | |
| TC-ERR-201 | graphviz 未安装降级 | | | | | | | |
| TC-NF-001 | 核心功能响应时间 | | | | | | | |
| TC-NF-002 | 知识库首次加载 / 缓存命中 | | | | | | | |
| TC-NF-101 | UI 视觉一致性 | | | | | | | |
| TC-NF-201 | 学术正确性抽查 | | | | | | | |
| TC-NF-202 | 敏感内容拦截 | | | | | | | |
| TC-DS-001 | 数据结构课程章节完整性 | | | | | | | |
| TC-DS-002 | 代码示例可运行性 | | | | | | | |
| TC-DS-101 | 高等数学课程章节完整性 | | | | | | | |
| TC-DEP-001 | Streamlit 启动冒烟 | | | | | | | |

### 10.2 缺陷登记模板

| 问题编号 | 严重程度（高/中/低） | 问题描述 | 复现步骤 | 影响模块 | 修复状态 | 修复日期 | 修复人 |
|---------|-------------------|---------|---------|---------|---------|---------|-------|
| BUG-001 | | | | | 待修复/已修复 | | |
| BUG-002 | | | | | | | |

**严重程度定义**：
- **高**：系统崩溃 / 核心功能不可用 / 数据丢失 / 安全风险
- **中**：功能异常但有替代方案 / UI 严重错乱 / 性能显著不达标
- **低**：轻微 UI 问题 / 文案优化建议 / 性能微小波动

---

## 第十一部分：辅助测试脚本

### 11.1 知识库数据完整性检查脚本

```bash
cd /home/hjj/桌面/t/A3-main
python -c "
from src.core.knowledge_base import list_courses, get_knowledge_base

courses = list_courses()
print(f'发现 {len(courses)} 门课程:')
for c in courses:
    print(f'  - {c[\"id\"]}: {c[\"course_name\"]} ({c[\"course_name_en\"]})')
    kb = get_knowledge_base(c['id'])
    chapters = kb.list_chapters()
    print(f'    章节数: {len(chapters)}')

    total_kp = 0; total_code = 0; total_q = 0
    for ch in chapters:
        data = kb.get_chapter(ch['id'])
        if not data:
            print(f'    ⚠  章节 {ch[\"id\"]} 数据为空或未找到')
            continue
        total_kp   += len(data.get('key_points', []))
        total_code += len(data.get('code_examples', []))
        total_q    += len(data.get('questions', []))
        assert 'summary' in data, f'{ch[\"id\"]} 缺少 summary'
        assert isinstance(data.get('key_points'), list), f'{ch[\"id\"]} key_points 非列表'
    print(f'    知识点: {total_kp}, 代码示例: {total_code}, 练习题: {total_q}')
    print(f'    抽样 JSON 解析: OK')
print('\\n知识库数据完整性: PASS ✓')
"
```

### 11.2 数据库 CRUD 自检脚本

```bash
python -c "
import tempfile, os
from src.core.database import Database

db_path = tempfile.mktemp(suffix='.db')
db = Database(db_path=db_path)
db.create_tables()

pid = db.add_student_profile('u1', [0.7,0.6,0.8,0.5,0.6,0.7], {'major': '计算机', 'weak_points':['链表']})
print(f'添加画像, id={pid}')

p = db.get_student_profile('u1')
assert p and p['feat1'] == 0.7
assert isinstance(p['extra'], dict) and p['extra']['major'] == '计算机'
print(f'查询画像 extra={p[\"extra\"]} ✓')

db.add_learning_step('u1', 2, 0.0, 'Step 2')
db.add_learning_step('u1', 1, 0.0, 'Step 1')
db.add_learning_step('u1', 3, 0.0, 'Step 3')
path = db.get_learning_path('u1')
assert len(path) == 3 and path[0]['step'] == 1 and path[2]['step'] == 3
print(f'查询路径: {len(path)} 步, 已排序 ✓')

db.add_resource('u1', 'r1', 'mindmap', 'content...', {'topic':'二叉树','chapter':'ch05'})
r = db.get_resource('r1')
assert r and r['resource_type'] == 'mindmap'
assert isinstance(r['metadata'], dict) and r['metadata']['topic'] == '二叉树'
print(f'查询资源: {r[\"resource_id\"]} ✓')

os.unlink(db_path)
print('\\n所有 CRUD 操作: PASS ✓')
"
```

### 11.3 思维导图渲染脚本

```bash
python -c "
import json, os, tempfile
from src.core.resource_generator import ResourceGenerator

rg = ResourceGenerator(work_dir=tempfile.gettempdir())
struct = {'title': '测试知识树',
          'children': [{'title': '基础概念', 'note': '包含定义与术语'},
                       {'title': '应用场景', 'children': [{'title': '场景 A'}, {'title': '场景 B'}]}]}
out = rg.render_mindmap(struct, output_name='unittest_mindmap', size='8,5', dpi=150)
print(f'生成文件: {out}')
size = os.path.getsize(out)
print(f'文件大小: {size} bytes')
assert size > 5_000, f'PNG 文件过小（{size} bytes），可能渲染失败'
print('渲染测试: PASS ✓')
os.unlink(out)
"
```

### 11.4 Streamlit 启动冒烟脚本

```bash
# 终端 1：后台启动（超时 30s 自动杀）
cd /home/hjj/桌面/t/A3-main
streamlit run src/frontend/app.py --server.headless=true --server.port=8765 &
STREAMLIT_PID=$!

# 终端 2（或 sleep 后）：健康检查
sleep 8
STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8765)
echo "HTTP Status: $STATUS"

kill $STREAMLIT_PID 2>/dev/null
wait $STREAMLIT_PID 2>/dev/null

if [ "$STATUS" = "200" ]; then
    echo "Streamlit 启动冒烟: PASS ✓"
else
    echo "Streamlit 启动冒烟: FAIL ✗ (返回 $STATUS)"
fi
```

---

**文档版本**：v2.1  
**更新日期**：2026 年 6 月  
**适用版本**：A3-main `HEAD`  
**文档编写遵循原则**：以 A3.md 第 3、4、5 项功能/非功能需求为经，以 A3.md 第 8 项"需求层面 + 技术开发层面"为纬，系统性验证多智能体学习资源系统的质量。